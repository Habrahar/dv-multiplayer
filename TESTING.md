# Что проверить — версия 0.1.21.1

**Версии должны совпадать у обоих.**

## Как снимать логи — это уже дважды мешало разбору

**Обе копии игры пишут в один и тот же `Player.log`** — папка LocalLow общая для всех
установок. Поэтому присланные «клиентский» и «хостовый» логи оказались одним файлом с
перемешанным выводом двух процессов.

**Чтобы получить раздельные логи, запускай второй экземпляр с ключом:**

```
DerailValley.exe -logFile "C:\путь\Player_Client.log"
```

Проверить, что лог хостовый: в нём есть `[Server]` и `[Diag] Licences: … joins`, и **нет**
`CacheWorldItems` (она бывает только у клиента).

**И включи Debug Logging в настройках мода.** Весь предметный поток у меня в дебаге —
без него по предметам в логе почти пусто, и разбор наполовину гадание.

## Новое в 0.1.21.1

- [ ] **Б25: предметы больше не дёргаются.** Моя регрессия из 0.1.21.0. Толкни предмет в
      кабине — должен ехать плавно, без мелкой тряски.
- [ ] **Б23: ушёл и вернулся.** Тот самый твой случай: отойди клиентом за 100 м, вернись
      (или запрыгни в вагон к хосту) — предметы должны быть **на месте и видны**. Раньше хост
      навсегда считал, что они у тебя уже есть, и больше не присылал.
- [ ] **Б24: отказ теперь исправляет.** Если сервер откажет в действии с предметом, твоя
      копия должна **сама вернуться к правде**, а не остаться в руках. В логе хоста при этом
      будет `Player action validation failed for ItemNetId`.

## Из 0.1.21.0 (проверено частично)

- [ ] **Б22: предметы в движении теперь стримятся.** Главное. Положи предмет на пол кабины и
      поезжай — у второго он должен лежать там же. Толкни предмет на ходу, чтобы он покатился:
      у второго он должен катиться **так же**, а не по-своему. Раньше отправлялось только
      «предмет уронили», и дальше каждый крутил свою физику.
      Стрим идёт, **только пока предмет движется**, и только тем, кто ближе 100 м.
- [ ] **Не должно быть регрессии:** брось предмет на землю — должен лететь и падать как
      раньше, без рывков и без «прыжка назад».
- [ ] **Б21, частично:** бланк джоба теперь создаётся при входе **только владельцу**.
      Но корень не починен: **пришли хост-лог** после того, как возьмёшь джоб хостом. Ищу в нём
      строку `CreateJobBooklet() NetworkedJob not found for Job ID` — она подтвердит или
      опровергнет мою гипотезу. И посмотри, какой `owner id` у джоба в клиентском логе:
      если снова `0` — корень там.

---

# Проверено 17.07 на 0.1.20.0

- Б18 — **починено**: ноль `NetworkedItem not found on client` (было 19).
- Б19 — **работает**: бумажка отказа печатается, таймаутов нет.
- Б20 — ноль падений в мировые координаты, но одного его **не хватило** (см. Б22).

## Новое в 0.1.20.0

- [ ] **Б19: отказ в джобе печатает бумажку.** Клиентом возьми джоб, на который **не хватает
      лицензий** — вместо голого звука должен напечататься отчёт с причиной, тот самый, что
      хост получал всегда. То же для «нет свободных слотов» (набери лимит джобов).
      На «джоб уже занят другим» бумажки не будет — её нет и в ванилле; останется звук.
      В логе клиента: `[Diag] Jobs: <id> refused - LicencesMissing. Printing the report`.
      Раньше в логе было `Job Validation Response timed out ... Rejected` — **этой строки
      быть больше не должно**, сервер теперь отвечает.
- [ ] **Б18: вход в идущую сессию.** Хост берёт джоб → **потом** клиент заходит. У клиента
      должен появиться бланк джоба там, где он лежит у хоста. В логе клиента больше **не
      должно** быть `NetworkedItem not found on client! ItemNetId: ...` — раньше сыпалось
      19 раз за 5 минут.
- [ ] **Б20: предметы в движущемся поезде.** Главное в этой версии. Положи предмет на пол
      кабины, поезжай — у второго игрока предмет должен лежать **на том же месте кабины**, а
      не уползать. Проверь на ходу: положил на ходу → у второго на том же месте. Брось предмет
      внутри едущей кабины — должен улететь примерно туда же.
      Предметы на земле должны работать как раньше — это регрессия, которую стоит проверить
      отдельно.
      В логе тревожно: `car ... not found; placing in world space instead`.

---

# Что проверить — версия 0.1.19.0 (проверено 17.07)

Ветка `feature/item-sync`. Личные профили (кошельки, лицензии, джобы) уже влиты в `beta`.

**Статус.** Бумажки и джобы синхронизируются — проверено в игре, лог хоста чистый. В 0.1.19.0
закрыты **шесть** предметных багов: Б16 (зависание в воздухе), Б6 (подмена отправителя),
Б5 + включение валидации, Б10 (выход игрока), Б14 (ретрансляция третьим), Б15 (фонарь).
Ничего из этого в игре не гонялось.

## Главное в этом прогоне — 0.1.19.0

- [ ] **Б16: личная вещь больше не висит в воздухе.** Тот самый твой случай: кинь личную
      вещь, второй подбирает, забери обратно в инвентарь, потом снова выкинь. Предмет должен
      **падать**, а не зависать. Это была не игра, а мы: предмет в инвентаре игры не спрятан,
      а **заморожен** (`isKinematic`), и мы его не размораживали.
- [ ] **Валидация включена — главный риск прогона.** Теперь сервер может **отказать** в
      действии с предметом. Ожидание: ничего не ломается. Если предмет вдруг «не берётся»
      или «не кладётся» — ищи в логе хоста `ValidatePlayerAction() ... Refused`, там будет
      имя, дистанция и причина. **Шли эту строку сразу.**
      Защита от залипания: предмет без владельца пропускается всегда, а владелец всегда может
      свой предмет положить — так что «застрял в руках навсегда» наступить не должно.
- [ ] **Б10: выход игрока.** Возьми предмет в руки/инвентарь **клиентом** и выйди из игры
      (Alt+F4 тоже годится). У хоста предмет должен **появиться на земле** в точке выхода.
      В логе хоста: `[Diag] Items: <ник> left, returned N carried item(s) to the world.`
      Раньше предмет пропадал из мира навсегда.
- [ ] **Б15: фонарь.** Зажги/потуши фонарь и покрути фитиль — второй игрок должен видеть
      огонь и его размер. Патч был закомментирован; включён так же, как это сделал автор
      мода в своей ветке.
- [ ] **Б14 — проверяется только втроём.** Вдвоём невидимо. Если будет третий: A берёт
      предмет, B должен увидеть, что предмет исчез. Раньше узнавал только хост.

**Чего в этой версии НЕ чинили:** Б17 (`RespawnOnDrop` морозит и прячет предметы по своим
локальным меркам). Он латентный — пороги 200 м для личных вещей, 1000 м для прочих. На таких
дистанциях предмет тебе всё равно не сервер шлёт (`MAX_DISTANCE_TO_ITEM = 100`). Ждёт решения
по варианту A/B/C, разбор в Plan.md.

---

Ниже — то, что накопилось раньше и всё ещё не проверено.

## Как снимать логи

Логи мода — в консоли **Unity Mod Manager, `Ctrl+F10`**. Удобнее файлом: включи в настройках
мода **Enable log file**, тогда рядом с модом появится `multiplayer.log`.

Всё, что мне нужно, помечено **`[Diag]`**. Отфильтруй по этой строке и скинь.

**Логи нужны с обеих машин** — часть решений принимает сервер, часть клиент, и интересно
именно расхождение. Подпиши, где чей.

Полезно перед каждой проверкой писать в чат мода что-нибудь вроде «--- тест 3 ---»: в логе
будет видно, где что начинается.

---

## 1. Джобы: у каждого свои

Самое новое и самое рискованное.

- [ ] **Клиент берёт джоб.** У хоста он **не** появляется, бланк у хоста **не** печатается.
      Ожидаю в логе: `[Diag] Jobs: <id> taken by <ник> ... No booklet printed on the host`
      и у второго игрока `mine: False ... Dropping the overview only`.
- [ ] **Второй игрок пытается взять тот же джоб.** Не должен.
      Ожидаю: `[Diag] Jobs: refused ... already taken by someone else`.
- [ ] **Бумажка пропадает у всех**, включая того, кто держит её в руках.
- [ ] **Цели идут.** Взял джоб — задачи двигаются, джоб можно довезти и сдать.
- [ ] **Лимит джобов личный.** Набери джобов до отказа: должно упереться в **твой** лимит
      (1 без лицензий на параллельные джобы, 2 с первой, без ограничения со второй),
      а не в общий на всех. Ожидаю: `refused ... no free slots (N/M)`.
- [ ] **Выход и вход.** Взял джоб, вышел, зашёл — джоб остался твой.
      Ожидаю: `[Diag] Jobs: joining onto in-progress <id> ... mine: True`.

**Известно заранее, багом не считать:** отказ в выдаче джоба **молчит** — просто звук ошибки
через 3 с, причины на экране нет (Б8). Причина будет в логе. И после **перезапуска хоста**
владение теряется (Б9).

## 2. Награда за джоб

- [ ] **Клиент сдаёт джоб.** Деньги приходят **ему** в кошелёк.
      Ожидаю: `[Diag] Pay: job <id> paid $X straight into <ник>'s wallet`.
- [ ] **Купюр у валидатора нет** ни у кого, включая хоста. Это осознанная цена решения.
- [ ] **Хост сдаёт свой джоб** — деньги ему, тоже без купюр.

## 3. Лицензии

- [ ] **Покупка не утекает.** Купил на одном — у второго не появилась. *(уже подтвердил)*
      Ожидаю: `[Diag] Licences: general '<id>' granted to <ник> only`.
- [ ] **Новый игрок.** Заходит впервые: `TrainDriver`, `DE2`, `FreightHaul` и $1000.
      Ожидаю: `[Diag] Licences: <ник> joins for the first time, with the starting set ...`.
- [ ] **Персист.** Купил, вышел, зашёл — лицензия на месте.
      Ожидаю: `joins with their saved licences - general [...]`.
- [ ] **Локомотив по лицензии.** У хоста есть лицензия на тепловоз, у тебя нет — ты в кабину
      **не** заходишь, он заходит. Купил — пускает сразу, без перезахода.

## 4. Отказ покупки лицензии — ни разу не проверен

Твой случай с $40 сюда не годится: там ванильный `Buy()` отказывает локально и до сервера
запрос не доходит. Нужен случай, когда клиент думает, что может, а сервер против.

**Как воспроизвести:** сделать **долг у хоста** (разбить/потерять вагон). Тогда сервер
откажет **клиенту**.

- [ ] На экране терминала появляется **причина** отказа, а не тишина.
- [ ] **Баланс не занизился** — вот это главное. Сервер возвращает авторитетную сумму.
      Ожидаю: `[Diag] Licences: refused <ник>'s purchase - OutstandingDebts. Returning their
      authoritative balance of $X`.

**Заранее известно:** долг у хоста блокирует покупку **всем** (Б2) — это и есть тот самый
случай, он же и баг. Чинится вместе с личными долгами.

## 5. Синхронизация предметов — включена, первый прогон

С 0.1.18.0 включено то, что было заглушено с B99: клиент постоянно прячет **свои** локальные
копии предметов мира, а видит предметы **сервера** (снапшоты пошли в обе стороны). Цель —
один экземпляр на мир. Валидация действий пока отключена — сервер верит клиенту на слово,
это следующий этап.

Как это устроено, чтобы понимать, что смотреть: игра сама прячет предметы дальше ~200 м и
сама же их показывает при приближении (`ItemDisablerGrid`), поэтому старая одноразовая уборка
при входе ничего не ловила — предметы появляются позже, по мере хода. Теперь уборка идёт
раз в секунду, всегда: всё локальное без серверного id прячется в пул.

**Прогон 0.1.18.0 уже показал:** уборка дублей и пул работают (спрятано 728, «re-hid 36»),
серверные предметы доезжают, взял/бросил у хоста — у клиента видно. Ниже — что осталось
и что добавилось после фиксов.

**Базовое:**

- [ ] Хост и клиент стоят у одного стола с предметами. У обоих предмет **один** и лежит
      **в одном месте**. (Раньше — у каждого своя копия.)
- [ ] Хост взял предмет в руку — у клиента он **пропал со стола**. Положил — появился,
      в том же месте у обоих. То же в обратную сторону: клиент взял — у хоста пропал.
- [ ] **Бросок теперь докатывается до одного места.** Кинь предмет и подожди пару секунд
      после остановки: у обоих он должен лежать в **одной точке** (хост досылает позицию
      покоя, когда физика заснула). В полёте небольшое расхождение — норма, но траектория
      должна быть **похожей**: скорость перед броском теперь обнуляется с обеих сторон.
- [ ] **Предмет не должен отскакивать назад** сразу после броска. Если увидишь, что он
      улетел и тут же прыгнул обратно к руке — сторож покоя сработал слишком рано, скажи.
- [ ] **Джоб-бумажка как предмет** (это чинилось): хост положил/бросил бумажку — у клиента
      она **переместилась туда же**. И наоборот. Если по бумажкам появится ошибка
      `missing FinaliseTrackedValues` в логе — шли сразу.
- [ ] Клиент отошёл от станции на пару клеток (300+ м) и вернулся — предметы на месте,
      **без дублей**. Это главный тест против `ItemDisablerGrid`.
- [ ] Перезаход клиента: вышел, зашёл — предметы на месте, без дублей.

**Что НЕ должно пострадать:**

- [ ] **Твои стартовые вещи** (лопата, фонарь, зажигалка и т.д. в инвентаре) на месте и
      работают. Выложил на землю, подобрал — не исчезают. (Личный инвентарь пока локальный.)
- [ ] **Джоб-бумажки живут по-старому**: оверлеи на станции есть, взял джоб — бумажка
      пропала у всех, бланк напечатался тебе. Джоб-система рулит бумажками сама, подавление
      их обходит стороной.
- [ ] **VR-игрок** (если есть возможность): взять/бросить работает так же.

**Ожидаемые странности (знаю заранее, интересен масштаб):**

- Предмет в **чужой руке не виден вообще** — он «в никуда» до этапа «предмет в руке модели».
  Взял в руку — у остальных исчез, положил — материализовался.
- **Огонь фонаря не синхронизируется** (сам фонарь как предмет — да): его патч выключен
  с B99 (Б15). Лопата (уголь), зажигалка и фонарик синхронизируют своё состояние.
- На большом удалении (>100 м) сервер предметы не рассылает — вблизи станции они должны
  «доехать» пакетом. Если у клиента столы **пустые** при подходе — это провал, шли лог.
- **Наличные из кассы** (внёс и забрал обратно) — под подозрением: если купюры спавнятся
  только локально у клиента, уборка съест их через секунду. Проверь: внеси в кассу, забери.
  Если исчезли — это известный кандидат в баги, шли лог, там будет видно.
- Покупки в **магазине** клиентом — та же история, проверь одну покупку.

**Логи:** раз в секунду при изменениях — `[Diag] Items: sweep suppressed N local item(s),
re-hid K ...`. При входе по-прежнему `CacheWorldItems() saw N, cached M`. Спам про каждый
снапшот убран в дебаг — если нужен полный поток, включи Debug Logging в настройках мода.
Красный флаг: `missing FinaliseTrackedValues` — шли сразу. Если что-то не так — шли всё
с фильтром `Items` с обеих машин.

---

## Диагностику потом убрать

Логи под `[Diag]` — временные, для этого прогона. Точек 14, найти легко:
`grep -rn "\[Diag\]" --include=*.cs Multiplayer/`

Исключение снято: настоящее подавление сделано периодическим свипом в
`NetworkedItemManager.SuppressLocalWorldItems` (хук в `ItemBasePatch` для него не подошёл:
игра включает уже-проснувшиеся предметы через `SetActive`, мимо `Awake`). `ReportLateItem`
остался чистой диагностикой — уйдёт вместе с остальными `[Diag]`.

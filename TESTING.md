# Прогон 0.1.25.1 — один сценарий по порядку

Двух игроков (хост + клиент) хватает на всё, кроме Б14 — он требует третьего и в этот прогон
не входит.

## Подготовка

1. **Версии одинаковые в обеих копиях** — формат пакета менялся, старый клиент с новым хостом
   не сойдётся.
2. В UMM (`Ctrl+F10`) → Multiplayer → **Show Advanced Settings**, под ней:
   **Enable Log File** и **Debug Logging** — в **обеих** копиях.
3. Логи забирать **рядом с `DerailValley.exe`**, а не из папки мода:
   `<папка установки>\multiplayer.log`. Файл чистится при каждом запуске.

Если что-то из пунктов ниже сломается — **остановись и пришли оба лога**. Дальше идти смысла
нет: следующий сбой будет уже не понять, чей.

---

## Часть 1. Мои регрессии — их чинил последними

Если тут плохо, остальное можно не начинать.

- [ ] **1.1 Предмет не дёргается.** Кинь предмет, смотри **клиентом**. Должен лететь плавно.
      *Было:* мелкая тряска у клиента, у хоста нормально.
- [ ] **1.2 Предмет не тонет в вагоне.** Зайди в кабину, кинь там предмет. Должен остаться в
      кабине.
      *Было:* дёргался и проваливался под землю.
- [ ] **1.3 Подошедший видит предметы.** Хост стоит у стола с предметами. Клиент отходит
      далеко (за 100 м), возвращается. Должен видеть **и старые предметы, и те, что хост
      кинул при нём**.
      *Было:* не видел ничего, иногда по две минуты.
      *Красный флаг в логе клиента:* `Update Type: Destroy` пачками (по 50+ разом).

---

## Часть 2. Данные — тут ошибка стоит дороже всего

- [ ] **2.1 Инвентарь помнится.** Клиентом купи что-нибудь в магазине, положи в пояс.
      **Выйди из игры, зайди обратно.** Вещь на месте?
      *Было:* всем на входе раздавался один зашитый набор, покупка исчезала.
      *В логе хоста:* `[Diag] Inventory: <ник> joins with their saved belt - N item(s)`
- [ ] **2.2 Выброшенное не возвращается.** Выкинь вещь из пояса, выйди-зайди. Её быть не
      должно.
- [ ] **2.3 Стартовый набор — только новичку.** Зайди клиентом с **новым GUID**
      (`Mods/Multiplayer/Settings.xml`, тег `<Guid>`). Должен приехать обычный набор.
      *В логе хоста:* `joins with the starting kit`
- [ ] **2.4 Заряд фонарика переживает выход.** Посади фонарик наполовину, выйди-зайди —
      заряд сохранился?
- [ ] **2.5 Джоб переживает перезапуск хоста.** ← *твой баг с мусоркой*
      Возьми джоб **хостом**, брось бланк на землю. **Перезапусти хост.** Зайди клиентом:
      бланк должен остаться хостовым, клиент **не должен** мочь выкинуть его в мусорку и
      отменить работу.
      *Красный флаг:* в логе клиента `owner id 0` у джоба в состоянии `InProgress`.

---

## Часть 3. Деньги

- [ ] **3.1 Фаст-тревел не бесплатный.** Телепортируйся **клиентом**. Деньги списались?
      Подожди полминуты — **не вернулись**?
      *Было:* списывались локально, следующий пакет баланса возвращал их обратно.
      *В логе хоста:* `[Diag] Money: <ник> fast travels to '<маркер>' for $N, $M left`
- [ ] **3.2 Цена совпадает** с той, что показала кнопка. Считает теперь хост.
- [ ] **3.3 Не хватает денег** — телепорт не состоялся?
      *В логе хоста:* `refused ... - $N but they hold $M`
- [ ] **3.4 Прыжок внутри своего состава** (из вагона в вагон одного поезда) — остался
      **бесплатным**?
- [ ] **3.5 Бумажка лицензии.** Купи лицензию **клиентом**. Бумажка вылезла из принтера и
      **не исчезла через секунду**?
- [ ] **3.6 Награда за джоб.** Сдай джоб клиентом — деньги пришли в кошелёк? Купюры при этом
      печататься **не должны** (так задумано).

---

## Часть 4. Что ни разу не гонялось

- [ ] **4.1 Выход с предметом в руках.** Возьми предмет **клиентом** в руки или пояс и выйди
      (Alt+F4 годится). У хоста предмет должен **появиться на земле** в точке выхода.
      *В логе хоста:* `[Diag] Items: <ник> left, returned N carried item(s) to the world.`
- [ ] **4.2 Фонарь.** Зажги/потуши фонарь, покрути фитиль — второй видит огонь и его размер?
- [ ] **4.3 Отказ в джобе печатает бумажку.** Клиентом возьми джоб без лицензий — должен
      напечататься отчёт с причиной, а не просто звук.
      *В логе клиента:* `refused - LicencesMissing. Printing the report`
      Строки `Job Validation Response timed out` быть **не должно**.
- [ ] **4.4 Валидация никого не блокирует.** Просто играй. Если предмет вдруг «не берётся»
      или «не кладётся» — ищи в логе хоста `ValidatePlayerAction() ... Refused` и шли строку.

---

## Часть 5. Регрессии — то, что работало и не должно сломаться

- [ ] **5.1 Предмет на земле.** Брось предмет на землю — летит и падает как раньше, без
      рывков и без прыжка назад?
- [ ] **5.2 Дублей предметов нет.** Отойди от станции и вернись — предметы одни, не двоятся.
- [ ] **5.3 Обычный джоб.** Возьми и сдай джоб клиентом без всяких перезапусков — работает?
- [ ] **5.4 Нет подтормаживаний** при ходьбе мимо станции.

---

## Отдельно: за чем присматривать

**Предметы могут тонуть в земле навсегда.** В 0.1.23.0 я снял `RespawnOnDrop` — страховку
игры на случай «предмет провалился сквозь пол» (Б17). Это принятая цена: компонент на каждой
машине по-своему морозил и прятал предметы, ничего никому не говоря. Но вживую эту цену никто
ещё не видел. Заметишь пропажу предмета в полу или земле — скажи, заведём свою страховку на
хосте.

---

## Что уже подтверждено в игре (перепроверять не нужно)

| Что | Чем |
|---|---|
| Б18 — бланк идущего джоба | 0 ошибок `NetworkedItem not found` вместо 19 |
| Б19 — бумажка отказа | печатается, таймаутов 0 |
| Б5/Б6 — валидация | 0 отказов за 9 минут |
| Б20 — координаты вагона | 0 падений в мировые координаты |
| Уборка дублей, пул | 728 предметов спрятано, `ItemDisablerGrid` пойман |

## Не проверить вдвоём

**Б14** — хост ретранслирует чужие действия третьим игрокам. Нужен третий: A берёт предмет,
B должен увидеть, что предмет исчез. Вдвоём этот путь не задействуется вовсе.

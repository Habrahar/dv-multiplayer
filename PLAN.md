# План: личные профили игроков в мультиплеере

Ветка: `feature/item-sync` (предметы). Личные профили влиты в `beta` форка.
Апстрим — `upstream` (AMacro), оригинал — `insprill`.

Модель, к которой идём: **мир общий, прогресс личный.** Предметы физически одни на всех,
деньги/лицензии/джобы у каждого свои.

---

## Сделано

### Личные кошельки (проверено в игре: у хоста 16k, у нового игрока 0)

| Что | Где |
|---|---|
| Кошелёк: `Money`, `AddMoney`, `RemoveMoney`, `SetMoney`, `IsHost` | `Multiplayer/Networking/Data/ServerPlayer.cs`, регион `#region Wallet` |
| Стартовые деньги новичка (1000) | `ServerPlayer.STARTING_MONEY`, применяется в `ClientboundSaveGameDataPacket.cs` |
| Адресная отправка баланса (была броадкастом) | `NetworkServer.SendMoney(ServerPlayer)` |
| Списание: удаление вагона, ререйл, рабочий поезд, лицензии | `NetworkServer.cs`, 4 места, ищи `player.RemoveMoney` |
| Списание: внесение наличных в кассу | `NetworkedCashRegisterWithModules.Server_ProcessCashRegisterAction` |
| Персист баланса по GUID | `NetworkedSaveGameManager.Server_UpdateInternalData` |

**Ключевое решение:** кошелёк хоста — это его собственный `Inventory` (им владеет
ванильный сейв), у остальных отдельное поле, персист в JObject по GUID. Второго
источника правды нет. Хост исключён из `Server_UpdateInternalData` — так было и до нас.

**Долг:** `CashRegisterBasePatch.AddCash` ищет получателя через `GetClosestPlayer` — см.
«Известные баги», Б1.

### Ответ на отклонённую покупку лицензии (в игре не проверено)

| Что | Где |
|---|---|
| Ответ RPC на все 3 ветки отказа + на успех | `NetworkServer.OnServerboundLicensePurchaseRequestPacket`, `DenyLicensePurchase` |
| Тип ответа (`UnknownLicense`, `OutstandingDebts`, `InsufficientFunds`) | `Multiplayer/Networking/Data/RPCs/LicensePurchaseResponse.cs` |
| `TicketId` в запросе | `ServerboundLicensePurchaseRequestPacket`, `NetworkClient.SendLicensePurchaseRequest` |
| Экран держится до ответа, показывает причину, таймаут | `Multiplayer/Patches/SaveGame/CareerManagerLicensePayingScreenPatch.cs` |
| Строки отказа | `Locale.cs` (`CAREER_MANAGER__LICENSE_REFUSED*`), `locale.csv` |

Взят готовый механизм тикетов (`RpcManager`/`RpcTicket`) — тот же, на котором работает
вызов рабочего поезда по рации. Свой велосипед не нужен.

**Ключевое решение:** отказ несёт обратно авторитетный баланс. Касса карьер-менеджера
(`CashRegisterCareerManager`) в моде не синхронизируется вообще — деньги уходят из
кошелька локально ещё при внесении. Без ресинка терминал бы починили, а игрок остался
с заниженным балансом до следующего денежного события. Это тот же рассинхрон в профиль.

Попутно вернул `return false` при неудачном локальном `Buy()`. Ванилла в этом случае
делает только `break`, так что `return true` заставлял вызвать `Buy()` дважды и звук
«не хватает денег» играл два раза. Причиной залипания эта строка не была.

**Проверено в игре (0.1.15.2):** кошельки у всех свои, терминал у каждого свой. Отказ от
сервера ещё не проверен — случай «внёс $40, не хватило» до сервера не доходит: ванильный
`Buy()` отказывает локально. Нужен долг у хоста (см. ниже).

### Чужая покупка больше не рисует по твоему терминалу (в игре не проверено)

`NetworkClient.OnClientboundLicenseAcquiredPacket` дёргал `PopulateTextsFromIndex` на
**всех** `CareerManagerLicensesScreen` в сцене. Лицензии общие, поэтому пакет приходит
всем, и список лицензий перерисовывался поверх того экрана, который игрок в этот момент
читал: уходя, `Disable()` тексты стирает, а возвращаясь, `Activate()` их сам заполнит.
Отсюда «зависший» терминал у второго игрока при чужой покупке.

Теперь обновляется только экран, который реально открыт — сверяемся с
`DisplayScreenSwitcher.CurrentScreen`. Покупателю обновление по-прежнему приходит: он на
этом экране и стоит.

### Награда за джоб — тому, кто сдал (в игре не проверено)

| Что | Где |
|---|---|
| Начисление в кошелёк сдавшего вместо печати купюр | `Multiplayer/Patches/Jobs/MoneyPrinterJobValidatorPatch.cs` |
| Проброс «кто сдал» через `ValidateJob` | `JobValidator_Patch.JobSubmitter` / `SetJobSubmitter` |
| Точка, где сервер знает отправителя | `NetworkServer.OnServerboundJobValidateRequestPacket`, ветка `JobBooklet` |

Ванилла платит физическими купюрами: `ValidateJob` → `moneyPrinter.PrintPayment(job)` →
через 3 с спавнит `Banknotes` у валидатора, игрок подбирает кошельком. Мод этот путь не
трогал вообще — ни одного упоминания `MoneyPrinter`/`PrintPayment`/`GetWageForTheJob`.

Беда в том, что бланк клиента валидирует **сервер**, поэтому купюры печатались в мире
хоста, и клиент их не видел — предметы не синхронизируются. Проверено вживую: игрок вернул
себе $40 из кассы, второй игрок этих купюр не видел.

**Ключевое решение:** печать купюр отключена (в мультиплеере; одиночная игра нетронута,
`IsSinglePlayer`), деньги идут прямо в кошелёк сдавшего. Физический путь честно заработает
только после оживления синхронизации предметов (п.1) — до тех пор показать купюры клиенту
нечем. Ритуал «забери наличку из диспенсера» при этом теряется, включая хоста.

### Личные лицензии (в игре не проверено)

| Что | Где |
|---|---|
| Наборы лицензий игрока + стартовый набор | `ServerPlayer.cs`, регион `#region Licenses` |
| Покупка клиента пишется игроку, а не хосту | `NetworkServer.OnServerboundLicensePurchaseRequestPacket` |
| Адресная выдача (была броадкастом) | `NetworkServer.SendLicense(ServerPlayer, id, isJob)` |
| Прогресс хоста больше не рассылается | `NetworkedSaveGameManager` — подписки на `LicenseAcquired`/`JobLicenseAcquired` убраны |
| Персист по GUID | `NetworkedSaveGameManager.Server_UpdateInternalData` |
| Личные лицензии при входе | `ClientboundSaveGameDataPacket` |

**Что было сломано:** сервер на покупку клиента звал `LicenseManager.Instance.Acquire*`, а это
**менеджер хоста**. Хост получал лицензию, которую не покупал, и событие рассылало её всем.
Плюс `ClientboundSaveGameDataPacket` отдавал каждому входящему лицензии хоста из сейва.

**Стартовый набор — не наш выбор, а требование игры.** `LicenseManager.LoadData` после
загрузки сама принудительно выдаёт `TrainDriver`, `DE2` и `FreightHaul`, если их нет, и
ругается в лог. Поэтому отдаём набор явно: `ServerPlayer.StartingGeneralLicenses` читает
`LicenseManager.TutorialGeneralLicenses`, а не хардкодит. К слову, ванилла при «скипнутом
туторе» даёт **$2000**, а у нас `STARTING_MONEY = 1000` — если захочешь сравнять, это здесь.

Клиент лицензии больше нигде для геймплея не спрашивает — сервер их не валидирует, проверено.

**Гаражи оставлены общими.** Их открывает физический замок в мире (`OnPadlockUnlocked` →
`UnlockGarage`), а не покупка, — на сервере нет момента, к которому привязать владельца.
Сделать их личными без серверного пакета «я открыл замок» нельзя, а это уже про рабочие
поезда, которые отложены.

### Личные джобы (в игре не проверено)

| Что | Где |
|---|---|
| Владелец джоба (Guid) + его id для провода | `NetworkedJob.OwnedBy` / `OwnerId` |
| Владелец в данных джоба | `JobData.OwnerId`, `JobUpdateStruct.OwnerId` |
| Выдача джоба по игроку, а не по хосту | `NetworkServer.Server_TakeJob` |
| Хост как владелец своих джобов | `JobValidator_Patch.ProcessJobOverview_Postfix` |
| Лицензии/слоты игрока | `ServerPlayer.IsLicensedForJob`, `AllowedConcurrentJobs`, `TakenJobCount` |
| Берёт джоб только владелец | `NetworkedStationController.HandleJobStateChange`, `AddJob` |
| Освобождение слота | `NetworkedJob.ReleaseFromOwner` |

**Что было сломано:** сервер звал ванильный `ProcessJobOverview` — тот судит **хоста**
(его лицензии, его слоты, его долги), кладёт джоб в `currentJobs` хоста и печатает бланк на
принтере хоста. А на клиентах `HandleJobStateChange` и `AddJob` звали `TakeJob(true)`
**у всех подряд** — поэтому джоб появлялся у каждого.

**Ключевое решение:** хост по-прежнему держит все джобы в своём `JobsManager` — это не
косметика, а движок: `JobsManager.Update` тикает задачи только тех джобов, что лежат в
`currentJobs`, а `CompleteTheJob` кидает исключение для чужих. Поэтому владение отдельно
(`OwnedBy`), а хранение общее. Клиенты же берут джоб только свой.

**Грабля, на которую наступил:** `TakeJob(job, takenViaLoadGame: true)` (нужен, чтобы не
будить долговой контроллер хоста за чужой джоб) **молча не помечает джоб dirty** — рассылка
бы не ушла вовсе. Отсюда явный `MarkStateDirty()`.

**Бумажки не требуют синка предметов.** Джоб-система сама их создаёт (`BookletCreator`) и
убивает (`DestroyJobOverview`) по job-пакетам — в том числе из рук. Это уже работало.

**Пути не колизятся** — генерация централизована: триггер по близости любого игрока
(`AnyPlayerSqrMag`), но джобы создаёт `StationController` хоста, один пул, а ванильный
`YardTracksOrganizer` резервирует пути между джобами. Не проверено вживую, но по устройству
двух джобов на один путь быть не должно.

**Долговой гейт для клиентов снят.** Ванилла проверяла `IsPlayerAllowedToTakeJob()` — долги
хоста. Для личного прогресса это неверно, поэтому в `Server_TakeJob` его нет. Пока личных
долгов не будет, у клиентов гейта по долгам нет вообще.

---

## Известные баги и грабли

Найдено чтением кода и в игре, **не** починено. Б-номера — чтобы ссылаться.

| # | Что | Где | Когда кусается |
|---|---|---|---|
| Б1 | `AddCash` платит **ближайшему** игроку, а должен — тому, кто применил кошелёк. У `GetClosestPlayer` вдобавок нет лимита дистанции: рядом никого — вернёт игрока хоть за 20 км | `CashRegisterBasePatch.AddCash`, `DvExtensions.GetClosestPlayer` | Сейчас |
| Б2 | Проверка долгов при покупке лицензии смотрит на синглтон игры, то есть на **долги хоста**. Есть долг у хоста — лицензию не купит никто. Лицензии стали личными, а этот гейт остался общим; чинится только личными долгами | `NetworkServer.OnServerboundLicensePurchaseRequestPacket` | Сейчас |
| Б3 | Клиент не получает физическую бумажку лицензии: ванилла печатает её в экране (`BookletCreator.CreateLicense` + `licensePrinter.Print()`), а патч это пропускает — лицензия приезжает пакетом, бумажка ниоткуда. Сама не закроется: предметы не синхронизируются (п.1) | `CareerManagerLicensePayingScreenPatch` | Сейчас |
| Б4 | Гаражи **общие**: открыл замок один — гараж есть у всех. Личными их не сделать без серверного пакета «я открыл замок» — это к рабочим поездам | `NetworkedSaveGameManager.Server_OnGarageUnlocked` → броадкаст | Сейчас |
| Б7 | ~~Взятие джоба судит хоста~~ — **закрыто в 0.1.17.0**, см. «Личные джобы» | — | — |
| Б8 | ~~Отказ в выдаче джоба молчит~~ — **закрыто в 0.1.20.0** вместе с Б19: три ветки отказа теперь шлют причину `ClientboundJobValidateResponsePacket`. Было: сервер логирует и выходит, клиент 3 с ждёт и играет звук ошибки, причины не видно. Та же болезнь, что чинили у покупки лицензий, — лечится тем же RPC-тикетом. **Подтверждено в игре 17.07:** сервер отказал в 22:13:00.328, клиент отвалился по таймауту в 22:13:01.514. Блокирует Б19 | `NetworkServer.Server_TakeJob` | Сейчас, с 0.1.17.0 |
| Б9 | **Владение джобом не переживает перезапуск хоста — и это корень Б21.** `OwnedBy` присваивается в `SetOwner()` и больше нигде не читается: ни сериализации, ни персиста. Джоб остаётся `InProgress`, владелец испаряется → работать его некому, взять заново нельзя, **а бланк становится ничей, и любой может выкинуть его в мусорку, отменив чужую работу**. Подтверждено логами: один джоб `HB-SL-32` — `owner id 1` в одной сессии, `owner id 0` в следующей | `NetworkedJob.OwnedBy`, персист в `NetworkedSaveGameManager` | **Сейчас. Самое критичное из открытого** |
| Б11 | **Быстрое перемещение бесплатное для клиента.** Ванилла списывает деньги из локального `Inventory` и на сервер не ходит — в логе хоста при телепорте за $961 нет ничего. Авторитетный баланс не меняется, следующий `SendMoney` возвращает деньги. Плюс проверка «хватает ли» идёт по локальному балансу, который сервер не контролирует. Лечится как покупка лицензии: серверный запрос | `FastTravelUIControllerPatch` трогает только кнопку | Сейчас |
| Б12 | **Guid игрока берётся из файла настроек мода** (`Settings.GetGuid`) и присылается клиентом. Две установки, склонированные друг из друга, имеют **одинаковый Guid** — а по нему идёт весь персональный персист (кошелёк, лицензии). С двумя игроками случайно работает, потому что хост из персиста исключён; с тремя — столкнутся. Ещё и подделывается тривиально. Файл: `Mods/Multiplayer/Settings.xml`, тег `<Guid>` | `Settings.GetGuid`, `ServerboundClientLoginPacket` | Сейчас |
| Б13 | **«Инвентарь игрока» — это хардкод.** `ClientboundSaveGameDataPacket` выдаёт каждому входящему один и тот же зашитый список — лопата, зажигалка, маслёнка, фонарь, фонарик, вешалка, скотч — в слоты 14–20. Чтения реального инвентаря там нет вообще. Выглядит как готовая фича, но это строительные леса: не принимай `PlayerItems` за персист инвентаря | `ClientboundSaveGameDataPacket.CreatePacket`, `StartGameData_ServerSave:65` | Сейчас |
| Б5 | ~~Проверка дистанции отвергнет законное действие~~ — **закрыто в 0.1.19.0**: дистанция проверяется только при свежем подборе (`currentOwner == null`). Было: взятый предмет деактивируется, transform замерзает на месте подбора, отошёл на 100 м → отказ: взятый предмет деактивируется, его transform замерзает на месте подбора. Отошёл на 100 м, переложил из рук в инвентарь → отказ. Похоже, это и есть настоящая причина заглушки. Лечится проверкой дистанции только при свежем подборе (`currentOwner == null`) | `NetworkedItemManager.ValidatePlayerAction`, `NetworkedItem.cs:622` | При снятии заглушки валидации (этап 3) |
| Б10 | ~~`PlayerDisconnected` кидает `NotImplementedException`~~ — **закрыто в 0.1.19.0**: сигнатура события сменилась в B99 на `Action<ServerPlayer>`, обработчик остался с `uint`. Теперь предметы ушедшего возвращаются в мир на месте выхода. Было, и подписки на него нет: событие `Server.PlayerDisconnect` пропало в B99 и его закомментировали. Предметы в руках вышедшего игрока остаются за ним навсегда — с живым синком они пропали из мира у всех | `NetworkedItemManager.cs:58,70` | **Сейчас, с 0.1.18.0** |
| Б6 | ~~`snapshot.Player` не сверяется с отправителем~~ — **закрыто в 0.1.19.0**: `snapshot.Player = player.PlayerId` в начале `ProcessReceivedAsHost`. Было, не сверяется с отправителем. Владение назначается по `snapshot.Player` уже сейчас (валидации-то нет). Эксплойт: уронить предмет, указав чужой id — владение останется за атакующим, предмет залочен навсегда. Лечится `snapshot.Player = player.PlayerId;` в начале `ProcessReceivedAsHost` | `ProcessReceivedAsHost`, `ItemUpdateData.cs:115` | Сейчас, с 0.1.18.0; чинить на этапе 3 |
| Б14 | ~~Хост не ретранслирует чужие действия третьим~~ — **закрыто в 0.1.19.0** через `MarkRelayDirty()` + датирование отправителя тем же тиком, чтобы бросок не вернулся к нему двойным импульсом. Было `ApplySnapshot` гасит dirty-флаги и не трогает `LastDirtyTick`, а рассылка смотрит только на них: игрок A бросил — хост применил, игрок B не узнает. Вдвоём невидимо, с тремя вылезет сразу. Досылка позиции покоя частично прикрывает (идёт всем), но `InHand`/`InInventory` от A до B не дойдут. Лечить пометкой dirty после применения на хосте — аккуратно с эхом отправителю: повторный `Throw` = двойной импульс | `NetworkedItem.ApplySnapshot`, `NetworkedItemManager.ProcessChanged` | С 3+ игроками |
| Б15 | ~~`LanternPatch` закомментирован~~ — **закрыто в 0.1.19.0**: раскомментирован, как это уже сделал апстрим в `feature-gadget-sync` (6cd9e24). Было — огонь и фитиль фонаря не синхронизируются (сам фонарь как тело — да). Раскомментировать и проверить API B99 | `Patches/World/Items/LanternPatch.cs` | Сейчас, с 0.1.18.0 |
| Б16 | ~~Предмет зависает в воздухе~~ — **закрыто в 0.1.19.0** вызовом `Inventory.Instance.ReturnItemToWorld(gameObject, false)`. Было Предмет в инвентаре игры — кинематический и на инвентарном слое (`Inventory.FinalizeRemoveItemFromWorld`). Мы при `Dropped`/`Thrown` делаем только `SetActive(true)` + позицию, а игровой `Inventory.ReturnItemToWorld` (`isKinematic = false` + `AssignWorldItemLayer`) не зовём. Итог: гравитация не действует, `AddForce` броска — пустая операция. Чинится строкой `Inventory.Instance.ReturnItemToWorld(gameObject, false)` до `SetActive`. Разбор ниже | `NetworkedItem.HandleDroppedOrThrownState` | **Сейчас** — личные вещи |
| Б17 | **`RespawnOnDrop` воюет с синком** (латентный: пороги 200 м личные / 1000 м прочие, при броске через комнату не стреляет). Игра на каждой машине сама морозит и прячет предметы по своим локальным меркам, ничего никому не говоря. Мод это знал: `RespawnOnDropPatch` написан, но **выключен первой же строкой**. Разбор ниже | `RespawnOnDropPatch`, `NetworkedItemManager.SendToCache` | На дистанции 200+ м |
| Б18 | ~~Бумажка джоба, взятого до твоего входа, не создаётся~~ — **закрыто в 0.1.20.0**: `GenerateBooklet` на ветке входа + `KnownItems` ставится только если `Create` ушёл. Было Вошёл в сессию, где джоб уже `InProgress` → ветка «joining onto in-progress» бланк **не печатает** (его печатает только `HandleJobStateChange` на живой смене состояния). При этом `ProcessChanged` ставит `KnownItems[item] = tick` **даже когда Create подавлен** `DoNotCreateItem` — хост считает, что бумажка у тебя есть, и шлёт `ItemState`/`FullSync` вечно. Доказано логом 17.07: netId 733, 19 ошибок за 5 мин. Разбор ниже | `NetworkedStationController.AddJob`, `NetworkedItemManager.ProcessChanged` | **Сейчас**, при входе в идущую сессию |
| Б19 | ~~Отказ в джобе не печатает бумажку~~ — **закрыто в 0.1.20.0**: клиент печатает тот же отчёт, что хост печатал всегда. Было `BookletCreator.CreateMissingLicenseReport()` → `JobMissingLicenseReport` существует в ванилле и в логе 17.07 реально спавнится у клиента. Но сервер на отказе молчит (Б8), клиент отваливается по таймауту и играет звук. Разбор ниже | `NetworkServer.Server_TakeJob` (3 ветки `return;`) | **Сейчас** |
| Б21 | ~~Бланк чужого джоба можно выбросить и отменить чужую работу~~ — **это не отдельный баг, а последствие Б9**: владение теряется при перезапуске хоста, джоб остаётся `InProgress` без владельца, бланк становится ничей. Чинится вместе с Б9. В 0.1.21.0 поправлена моя половина: `GenerateBooklet` создавал бланк всем, теперь только владельцу | см. Б9 | — |
| Б23 | ~~Ушёл за 100 м и вернулся — предметов нет~~ — **закрыто в 0.1.21.1**: `KnownItems` не чистился никогда, хост навсегда считал, что предмет у тебя есть, и `Create` не слал. Разбор ниже | `NetworkedItemManager.UpdatePlayerItemLists` | — |
| Б24 | ~~Отказ валидации ничего не исправляет~~ — **закрыто в 0.1.21.1**: отказ теперь помечает предмет грязным, отказанному уходит `FullSync` с правдой. Разбор ниже | `NetworkedItemManager.ProcessReceivedAsHost` | — |
| Б25 | ~~Стрим движения дёргает предметы~~ — **закрыто в 0.1.21.1**: скорость ведёт физика, позиция сглаживается, снап только при расхождении >0.5 м. Моя регрессия из 0.1.21.0 | `NetworkedItem.ApplyMotionStream` | — |
| Б22 | ~~Движущиеся предметы синхронизируются только по событию~~ — **закрыто в 0.1.21.0**: `ItemPosition` в одиночку стал потоком движения (вагон + позиция + поворот + скорость), хост стримит предмет, пока тот движется. **Формат пакета изменён.** Разбор ниже | `NetworkedItem.GetSnapshot`/`ApplyMotionStream`, `ItemUpdateData` | — |
| Б20 | ~~Позиция предмета в мировых координатах~~ — **закрыто в 0.1.20.0**: `Dropped`/`Thrown` несут `CarNetId` + локальные координаты вагона. **Формат пакета изменён.** Было У `Dropped`/`Thrown` в пакете только мировая позиция; `CarNetId` в `ItemUpdateData` **есть**, но пишется лишь для `Attached`. Пока пакет летит, поезд уехал, а копия вагона у клиента интерполируется отдельно → предмет ложится не туда. Игроки в моде **уже** синхронизируются в локальных координатах вагона — предметы нет. Разбор ниже | `ItemUpdateData.Serialize`, `NetworkedItem.CreateUpdateData` | **Сейчас**, в движении |

Б1 разбирать вместе с кассами. Б2 (долги) и Б4 (гаражи) — вместе с поездами (п.4): оба
упираются в личные долги и владение, а не в лицензии. Б3, Б5 и Б6 — в момент решения по п.1:
все три про синхронизацию предметов.

**Чему тут нельзя верить.** Этот план уже пять раз уверенно врал, и каждый раз это
вскрывалось только чтением реального кода игры через декомпилятор (`ilspycmd` + 
`Assembly-CSharp.dll` из установленной игры):

1. «Снять заглушку валидации — дёшево и фундаментально» → метод недостижим целиком (п.1).
2. «`AddCash` — это оплата джоба» → это внесение кошелька в кассу при **покупке**.
3. «`post-build.ps1` копирует мод сам» → из CLI молча не копировал.
4. «Предметы висят в воздухе из-за `RespawnOnDrop`» → механизм описан верно, но пороги там
   **200 м**, при броске через комнату он не запускается вовсе. Настоящая причина оказалась
   в нашем коде (Б16). Мало проверить, **что** делает метод, — проверь, **при каких числах
   он вообще срабатывает**.
5. «`owner id 0` у всех шести джобов — владелец не назначается никогда» → все шесть были
   `-> Expired`, то есть никем не взятые, и ноль для них **правильный**. Улика была одна, и
   вела она к давно записанному Б9. Цифра сама по себе не значит ничего — читай строку целиком.

Прежде чем брать отсюда утверждение о поведении игры — проверь его в декомпиляторе.

---

## Дальше по порядку

### 1. Синхронизация предметов

Цель: **один экземпляр предмета на мир.** Взял со стола — у остальных он со стола пропал.
Никаких «у каждого своя копия лопаты».

#### Главное: архитектура уже есть и она правильная

Это не проект с нуля. Модель заложена **серверно-авторитетная**, и она ровно та, что нужна:

| Кусок | Где | Состояние |
|---|---|---|
| Каждый предмет — `NetworkedItem` | `ItemBasePatch` — постфикс на `ItemBase.Awake` | Работает |
| Клиент прячет свои локальные предметы мира в пул при входе | `NetworkedItemManager.CacheWorldItems`, зовётся из `NetworkClient.cs:310` | Работает |
| Сервер шлёт авторитетный набор: `Create` / `Destroy` / состояние | `ProcessChanged` → `SendItemsChangePacket` | Работает |
| Клиент создаёт/убирает предмет по снапшоту, с пулом префабов | `ProcessReceivedAsClient`, `CreateItem`, `SendToCache`, `BuildPrefabLookup` | Работает |
| Состояния: `Dropped`/`Thrown`/`InHand`/`InInventory`/`Attached` | `NetworkedItem.ItemState` | Работает |
| Владение + проверка дистанции | `ValidatePlayerAction` | Написано, отключено |
| Раздача снапшотов по близости к игроку | `UpdatePlayerItemLists`, `MAX_DISTANCE_TO_ITEM = 100` | Работает |

**Выключено было ровно две строки** — оба вызова `ReceiveSnapshots` в
`OnCommonItemChangePacket` сервера и клиента. С 0.1.18.0 включены обратно.

#### Почему выключили — выяснено, и это меняет оценку риска

Коммит `b82d477 «Compatibility with B99»`, 24.11.2024. Прошлая версия плана писала «год не
работало, неизвестно что ломалось, большой риск». Посмотрел диф — там три вещи:

1. `GrabberRaycasterDV.SPHERE_CAST_MAX_DIST` → `RAYCAST_MAX_DIST` — переименование API игры.
   **Уже починено**, в коде новое имя.
2. `Server.PlayerDisconnect` — событие пропало, подписку закомментировали. `PlayerDisconnected`
   рядом кидает `NotImplementedException`. **Дыра: владение предметами при выходе игрока.**
3. `return;` в начале `CacheWorldItems` + оба `ReceiveSnapshots`.

То есть это была **не отмена по архитектурной причине, а заглушка на время миграции на B99**,
которую не доделали. Пункт 3 частично уже откатили: `41fabc5` убрал `return` из
`CacheWorldItems`, а `ReceiveSnapshots` не тронул.

#### Почему у каждого была своя копия — разгадано декомпилятором (16.07)

Я предсказал по коду, что у клиента не должно остаться лежащих предметов: он же прячет их в
пул, а `Create` от сервера не приходит. **Предсказание неверное** — в игре копия у каждого,
бросков-перекладок второй игрок не видит.

Разгадка в `DV.ItemDisablerGrid` + `DV.ItemDisabler` (движок игры, не мод): предметы мира
живут **выключенными**, пока игрок дальше своей клетки сетки 128 м (активны клетки 3×3),
и игра **сама включает их** при приближении. Отсюда две дыры сразу:

1. `CacheWorldItems` при входе видел только предметы возле точки спавна — остальные ещё
   выключены или вовсе не проснулись, прятать нечего.
2. Включение уже-проснувшегося предмета — это `SetActive(true)` **мимо `Awake`**, так что
   и постфикс на `ItemBase.Awake` в одиночку бы не спас. Ловить надо периодически.

Ещё из декомпиляции: `ItemDisabler` создаётся в конце кадра после `Awake` через глобальный
`CoroutineManager` (выключение предмета его не отменяет), но выключенные **не им** предметы
он не трогает — то есть спрятанное модом остаётся спрятанным, пока предмет не был выключен
самой игрой раньше (тогда она его включит обратно — и свип прячет его снова).

#### Сделано 16.07 (0.1.18.0, в игре не проверено): этапы 1 и 2

| Что | Где |
|---|---|
| Постоянная уборка: раз в секунду всё локальное без NetId — в пул | `NetworkedItemManager.SuppressLocalWorldItems` |
| Исключения из уборки: essential, в руке, в инвентаре, джоб-бумажки | `ShouldSuppressLocalItem` |
| Повторное прятание того, что игра включила обратно | там же, ветка `CachedItemSet` |
| Защита пула от двойного кэширования одного экземпляра | `CachedItemSet`, `SendToCache`/`GetFromCache` |
| Клиент не шлёт снапшоты предметов без NetId (сервер их не знает) | `ProcessClientChanges` |
| Оба `ReceiveSnapshots` включены — снапшоты пошли | `NetworkServer`/`NetworkClient.OnCommonItemChangePacket` |
| Фикс: `DoNotCreateItem(GetType())` сравнивал `NetworkedItem` с `JobOverview` — всегда false, `Create` для джоб-бумажек ушёл бы клиентам дублем | `ProcessChanged`, теперь `TrackedItemType` |
| `NetId` присваивается до `SetActive(true)` при создании по снапшоту | `CreateItem` |

**Ключевые решения.** Подавление — свип раз в секунду по `NetworkedItem.GetAll()`, а не хук
на `Awake`: хук не видит повторных включений (см. выше), а секунда невидимой задержки на
дистанции 128+ м глазу недоступна. Фильтр «локальный» = `NetId == 0`: на хосте id получает
каждый предмет при `Awake`, на клиенте — только от сервера (проверено по `IdMonoBehaviour`).
Джоб-бумажки узнаются по компонентам (`JobOverview` и родня): флаг essential у `JobBooklet`
появляется только через кадр, а `TrackedItemType` — после `Initialize`, компонент же есть
сразу.

**Известные риски прогона:** наличные, забранные из кассы клиентом, и покупки в магазине —
если они спавнятся только локально, свип их съест через секунду (у них никогда не будет
NetId). Проверка — в TESTING.md п.5. Если подтвердится — лечится серверным спавном купюр
(тот же путь, что «награда за джоб»).

**Осталось из плана этапа 2:** `PlayerDisconnected` — Б10, не тронут (подписки нет, не зовётся).

#### Первый прогон в игре (16.07, 0.1.18.0) и фиксы 0.1.18.1

**Что подтвердилось.** Фундамент живой: при входе клиент спрятал 728 предметов, серверные
`Create` доехали и применились, взял/бросил у хоста — у клиента видно. Механизм
`ItemDisablerGrid` пойман с поличным: «re-hid 36 cached item(s) the game re-activated».

**Что нашлось по логам (починено в 0.1.18.1):**

| Что | Где чинилось |
|---|---|
| Снапшоты джоб-бумажек гнили в очереди вечно: `Initialize()` ставит `UsefulItem`, а очередь разгружает только `FinaliseTrackedValues()` — для бумажек его не звал никто, ни хост, ни клиент. В логах — `Queuing` минутами по одним и тем же netId с обеих сторон. Положение бумажек не синхронизировалось вообще | `BookletCreatorPatch` (3 места), `NetworkedStationController` (3 места) |
| Финального положения не было в протоколе: бросок — это «повтори физику» (`grabHandler.Throw`), у каждого своя физика, куда докатилось — никто не сообщал. Теперь хост ждёт, пока rigidbody брошенного/уроненного заснёт, и досылает одну финальную позицию (`FullSync`) | `NetworkedItem.settleWatch`/`settleSyncDue` в `GetSnapshot`/`ApplySnapshot` |
| Бросок не воспроизводился одинаково: `GrabHandlerItem.Throw` — это `AddForce`, то есть **толчок к текущей скорости**, а не задание скорости. У бросающего предмет вылетает из руки покоящимся (пока держат — `isKinematic`), а у принимающего к тому же толчку добавлялась остаточная скорость. Теперь перед воспроизведением броска скорость и угловая скорость обнуляются — обе стороны стартуют из одного состояния | `NetworkedItem.HandleDroppedOrThrownState` |
| Гонка в сторожке покоя (мой же фикс): только что отпущенное тело ещё «спит», и сторож мог счесть это покоем на первом же тике — предмет отскочил бы назад к точке броска. Теперь ждём, пока физика видимо подхватит тело, с таймаутом 5 с на случай «не проснулось» или «не останавливается» (предмет на едущем вагоне) | `NetworkedItem.CheckSettled` |
| `CreateUpdateData` умеет вернуть `null`, а `ProcessChanged` клал его в пакет без проверки | `NetworkedItemManager.ProcessChanged` |
| `ApplySnapshot` не запоминал применённое состояние (`lastState`) — следующее локальное событие диффалось со протухшим | `NetworkedItem.ApplySnapshot` |
| Сторожок на будущее: 10 снапшотов в очереди без завершённой регистрации — ошибка в лог с прямым текстом «missing FinaliseTrackedValues» | `NetworkedItem.ReceiveSnapshot` |
| Пер-снапшотный спам (`ApplySnapshot`×3, `ProcessReceivedAsHost` warning) ушёл в LogDebug | там же |

**Найдено, не чинено:** Б14 (хост не ретранслирует третьим) и Б15 (`LanternPatch`
закомментирован) — оба в таблице багов выше.

**Как лог доказал диагноз по бумажкам.** netId 729/730/735 получали `Create` и снапшоты
**применялись** — это обычные предметы: они приходят через пул, где `Start()` давно отработал
и выставил `registrationComplete`. netId 742/743/744/747/751/756 не получали `Create`
**никогда**, только `ItemState`, и все — `Queuing` с обеих сторон. Это ровно бумажки:
`DoNotCreateItem` запрещает им `Create`, их создаёт джоб-система локально через `Initialize()`,
а `Initialize()` ставит `UsefulItem = true`, после чего `Start()` уже не имеет права выставить
`registrationComplete`. Единственный класс предметов, который был глухим — и именно его
кидали в тесте.

**Из того же лога, к старым багам:** Б11 виден вживую — клиент телепортировался «Порт и
город» за $959, в логе хоста тишина.

**Не баг:** `NullReferenceException` в `DV.UI.SettingChangeSourceSlider.OnResetOrApplied` и
`GetLobbyData() Error parsing property: GameMode` — ванильные болячки игры/лобби, к моду
отношения не имеют, в обоих логах до подключения.

#### Второй прогон (16.07, вечер, 0.1.18.1): джобы синхронизируются

**Работает.** Бумажки ожили, джобы синхронизируются, в логе хоста **ни одной** ошибки
`missing FinaliseTrackedValues` и вообще ни одной ошибки предметов. Лог теперь тихий —
пер-снапшотный поток ушёл в дебаг, как и задумано.

#### Третий прогон (17.07, 0.1.19.0) — лог чистый, но вскрылось четыре вещи

**Что подтвердилось хорошего.** Оба лога — от 0.1.19.0, то есть со вчерашними фиксами.

- **`ValidatePlayerAction` — ноль отказов.** Главный риск прогона не реализовался: валидацию
  включили, и она ничего не сломала. Ни одного `Refused` за 9 минут игры.
- Ни одной `missing FinaliseTrackedValues`. Бумажки живы.
- Б10 и Б14 в этом прогоне **не проверялись**: никто не выходил с предметом в руках, а
  игроков было двое (Б14 виден только втроём).
- Тормоза (`OnTick took 1936 ms`, `PollEvents took 3095 ms`) — **единичные**, в момент входа
  и удаления вагонов. Синк предметов ни при чём, раздувать не буду.

#### Б18: бумажка джоба, взятого до твоего входа

**Симптом в логе:** `NetworkedItem not found on client! Update Type: ItemState, ItemNetId: 733`
— 19 раз за 5 минут, и **только** этот netId.

**Доказано сопоставлением логов:**

| Время | Где | Что |
|---|---|---|
| 22:11:23.875 | хост | `Processing JobOverview HB-SL-32` — хост взял джоб |
| 22:11:32 | клиент | вход в сессию |
| 22:11:39.227 | клиент | `[Diag] Jobs: joining onto in-progress HB-SL-32, owner id 1, mine: False` |
| 22:12:13 → 22:17:26 | клиент | 19 × `NetworkedItem not found on client! ItemNetId: 733` |

**Механизм — две ошибки, которые встретились:**

1. `NetworkedStationController.AddJob()` на ветке «джоб уже идёт» бланк **не печатает** —
   печатает только `HandleJobStateChange()` на живой смене состояния, которой при входе нет.
   У клиента предмета с netId 733 просто не существует.
2. `ProcessChanged()` ставит `player.KnownItems[nearbyItem] = tick` **до и независимо** от
   того, отправился ли `Create`. А для бумажек `Create` подавлен `DoNotCreateItem`. Итог:
   хост уверен, что бумажка у клиента есть, и до конца сессии шлёт ей `ItemState`/`FullSync`.

Ошибка безобидна на вид (предмет и так не показать), но она означает, что **любой предмет,
чей `Create` не дошёл, навсегда становится «известным» и вечно генерирует трафик и ошибки**.

**Как чинить — две стороны, нужны обе:**
- Печатать бланк при входе на идущий джоб — там же, где `GenerateOverview` печатает обзор,
  с тем же `ItemNetID` из `JobData`. Тогда 733 у клиента появится по-честному.
- В `ProcessChanged` ставить `KnownItems` **только если `Create` реально ушёл**. Иначе та же
  болезнь повторится с любым предметом, у которого `CreateUpdateData()` вернул `null`.

#### Б19: бумажка отказа — игра это уже умеет

**Ты спросил: можно ли печатать бумажку с причиной вместо звука. Ответ: да, и почти всё есть.**

В ванилле есть готовый механизм:
`BookletCreator.CreateMissingLicenseReport(Job job, bool isJobLicenseMissing, Vector3 pos,
Quaternion rot, Transform parent)` → `BookletCreator_JobMissingLicense.Create(...)` →
предмет `JobMissingLicenseReport`. Мод про этот тип **уже знает** — он перечислен и в
`DoNotCreateItem`, и в `ShouldSuppressLocalItem`, и в `NetworkedJob`.

**И он живой.** В логе 17.07 клиент реально его заспавнил:
`[22:13:08.120] [Diag] Items: 20 item(s) have appeared... Newest: JobMissingLicenseReport`.

**Блокер — Б8, и лог показывает его в упор:**

```
[22:13:00.328] [Server]  refused Habrahar1 job HB-SU-05 — THEIR licences do not cover it
                         (Shunting, TrainLength1, Fragile)
[22:13:01.514] [Client]  Job Validation Response timed out for HB-SU-05. Rejected
```

Сервер отказал и **промолчал**: в `NetworkServer.Server_TakeJob` три ветки отказа, и каждая —
`LogWarning(...); return;`. Клиент ждёт ~3 с, отваливается по таймауту и играет звук. Причина
на сервере известна с точностью до списка лицензий, но никуда не едет.

**Три причины отказа уже различаются на сервере:**
1. `already taken by someone else` (`netJob.OwnerId != 0`)
2. `THEIR licences do not cover it` (`!player.IsLicensedForJob(job.requiredLicenses)`)
3. `no free slots` (`TakenJobCount >= AllowedConcurrentJobs`)

**Как чинить — механизм уже написан в этом же репозитории.** Отказ покупки лицензии чинили
ровно этим: RPC-тикет (`RpcManager`/`RpcTicket`), `LicensePurchaseResponse` с типом причины,
экран держится до ответа и показывает причину. Здесь нужен тот же приём: `JobTakeResponse` с
причиной → клиент печатает бланк.

**Поправка к самому себе:** я написал выше, что «для нет слотов бумажки в ванилле нет». Это
неверно — декомпиляция `JobValidator.ProcessJobOverview` показала готовую таблицу, и бумажка
там **одна на оба случая**, а различает их флаг:

| Условие в ванилле | Бумажка | `isJobLicenseMissing` |
|---|---|---|
| `currentJobs.Count >= GetNumberOfAllowedConcurrentJobs()` | `CreateMissingLicenseReport` | **false** |
| `!IsLicensedForJob(...)` | `CreateMissingLicenseReport` | **true** |
| `!IsPlayerAllowedToTakeJob()` (долги) | `CreateDebtWarningReport` | — |
| фича-флаг выключен | `CreateTutorialWarningReport` | — |

Своего текста не нужно вовсе. Нет ванильной бумажки только у «уже занят» — и не может быть:
в одиночке некому занять джоб раньше тебя.

**Сделано в 0.1.20.0.**

#### П.3: «далеко → не обновляется» — механизм уже есть, и он правильный

**То, что ты описываешь, уже реализовано именно так:**

| Твоя формулировка | Как называется в коде |
|---|---|
| «если я в радиусе — узнаю про грязные предметы» | `NearbyItems` + `MAX_DISTANCE_TO_ITEM = 100 м` |
| «если ухожу — забываю, чтобы упростить отправку» | `NEARBY_REMOVAL_DELAY = 3 с`, предмет выпадает из `NearbyItems` |
| «подхожу — узнаю, что изменилось» | `KnownItems[item] < item.LastDirtyTick` → досылка `FullSync` |

**И одна из дыр в этом уже закрыта вчера.** Б14 был ровно про это: применение чужого снапшота
гасило флаги и **не двигало `LastDirtyTick`**, поэтому вернувшийся игрок не догонял чужое
действие. `MarkRelayDirty()` это чинит — но лог, который ты прислал, снят уже с этим фиксом.

**Честно: твой симптом по логу я подтвердить не смог.** Единственная предметная ошибка в
логе — netId 733, а это бумажка (Б18), которой у клиента вообще нет. Возможно, ты видел
именно её.

**Что в этом механизме действительно дырявое (найдено чтением, не логом):**

- **`KnownItems` не чистится никогда.** Игрок ушёл — предмет остаётся «известным» навсегда.
  Словарь растёт всю сессию; чистится только на `Destroy` предмета и (с 0.1.19.0) на выходе
  игрока. Это и память, и линейные сканы.
- **Цикл удаления из `NearbyItems` бракованный:**
  ```csharp
  for (int i = 0; i < player.NearbyItems.Count; i++) {
      var kvp = player.NearbyItems.ElementAt(i);
      if (currentTime - kvp.Value > NEARBY_REMOVAL_DELAY) player.NearbyItems.Remove(kvp.Key);
  }
  ```
  `Remove` внутри индексного обхода по `ElementAt(i)` **пропускает элементы**: после удаления
  коллекция сдвигается, а `i` растёт. За один проход вычищается примерно половина. Плюс
  `ElementAt(i)` по словарю — это O(n) на каждый шаг, то есть цикл O(n²).
- **Б18** — прямое следствие: `KnownItems` ставится без отправки `Create`.

**Вывод:** отдельной задачи «сделать радиус» не нужно — она сделана. Нужно починить Б18 и
цикл удаления, а потом перепроверить симптом. Если после этого предметы **не бумажки** всё
ещё не догоняются — вот тогда копать, и уже с включённым Debug Logging.

#### Б20: предметы в движущемся поезде — синхронизируем не в той системе координат

**Ты прав, и подход в моде действительно не тот. Причём правильный — уже рядом, для игроков.**

Что шлётся сейчас (`ItemUpdateData.Serialize`):

| Состояние | Что в пакете |
|---|---|
| `Dropped` / `Thrown` | `ItemPosition` (**мировая**) + `ItemRotation`. Вагона нет |
| `Attached` | `CarNetId` + `AttachedFront`. **Позиции нет** |
| `InHand` / `InInventory` | `Player` |

`CreateUpdateData()`: `position = transform.position - WorldMover.currentMove` — мировые
координаты за вычетом сдвига мира. Поле `CarNetId` в структуре **уже есть**, но для
`Dropped`/`Thrown` не пишется.

**Почему в движении разъезжается — три причины сразу:**

1. **Кадр отсчёта не тот.** Пока пакет летит (~40 мс на тик + пинг), поезд уехал на метры.
   Клиент ставит предмет в мировую точку, которая к вагону уже не имеет отношения. Хуже:
   копия вагона у клиента интерполируется своим темпом, то есть стоит **не там**, где вагон
   у хоста.
2. **Каждый считает свою физику.** Внутри вагона `CabItemRigidbody.ReceiveForcesFrom`
   скармливает предмету силы поезда — это неинерциальная система отсчёта, где траектории
   расходятся мгновенно.
3. **Сторож покоя (мой, 0.1.18.1) тут бессилен:** на едущем поезде тело может не заснуть
   никогда → срабатывает таймаут 5 с → досылается мировая позиция, которая уже устарела.

**Как это делают в проектах, где так не разъезжается:** синхронизируют не мировую позицию, а
позицию **в системе отсчёта родителя** — «предмет лежит в кабине L-050 в точке (0.3, 1.1, 2.0)
относительно вагона». Тогда движение поезда не участвует в синхронизации вообще: вагон едет,
предмет едет с ним, а по сети идёт неизменная локальная координата.

**И это уже сделано в моде — для игроков.** `ServerPlayer.WorldPosition`:

```csharp
if (CarId == 0 || !NetworkedTrainCar.TryGet(CarId, out car))
    pos = RawPosition + WorldMover.currentMove;      // в мире — мировые
else
    pos = car.transform.TransformPoint(RawPosition); // в вагоне — ЛОКАЛЬНЫЕ вагона
```

Игрок в вагоне трекается в локальных координатах вагона, и поэтому не «плавает» при движении.
Предметы этого не делают.

**Что делать (задача, не сейчас):**
1. Определять, в каком вагоне лежит предмет — `TrainCar.Resolve(transform)` уже используется
   в игре (`RespawnOnDrop.UpdateSpawnParams`, `ItemBase.SetupTrainReceivingForces`).
2. Для `Dropped` внутри вагона писать в пакет `CarNetId` + **локальную** позицию/поворот
   (поле `CarNetId` уже есть — сериализацию расширить на `Dropped`/`Thrown`).
3. На приёме: `car.transform.TransformPoint(localPos)`, а лучше — парентить предмет к вагону,
   как это делает сама игра.
4. Сторож покоя должен считать покой **относительно вагона**, а не мира.

Это самая большая из предметных задач и трогает формат пакета — то есть ломает совместимость
версий. Делать отдельным заходом, не вперемешку.

#### Четвёртый прогон (17.07, 0.1.20.0): Б18/Б19 закрыты, Б20 оказался половиной решения

**Подтверждено в игре:** ноль `NetworkedItem not found` (было 19), ноль падений в мировые
координаты, а Б19 работает целиком: `Jobs: HB-SU-52 refused - LicencesMissing. Printing the
report`, бумажка печатается, **таймаутов больше нет ни одного**.

**Внимание: оба присланных лога оказались клиентскими** (`CacheWorldItems` есть в обоих,
`Jobs: refused` и `Licences: … joins` — ни в одном). Хост-лог для разбора нужен отдельно.

#### Б21: бланк чужого джоба можно выбросить и отменить чужую работу

Найдено в игре 17.07: хост взял джоб и бросил бланк, клиент вошёл, увидел бланк и выкинул
его в мусорку — **джоб хоста отменился**. Это ровно тот саботаж, о котором предупреждал
открытый вопрос 1, только теперь он воспроизведён.

**Что видно в логе (клиентском, но данные пришли с сервера):**

```
[Diag] Jobs: joining onto in-progress HB-SL-32, owner id 0, mine: False
```

**`owner id 0` — у джоба, взятого хостом, нет владельца.** Отсюда всё остальное: бланк ничей,
защищать его некому. Плюс `GenerateBooklet` не вызывался вовсе (нет строки в логе) — значит
`ItemNetID` был 0, то есть у хоста `networkedJob.JobBooklet` тоже пустой.

**Главная гипотеза (в хост-логе не проверена, нужен лог):** в `BookletCreatorPatch.CreateJobBooklet`
упал `TryGetFromJob` → `LogError("CreateJobBooklet() NetworkedJob not found for Job ID")` и
`Initialize()` **не вызвался**. Тогда сходится всё сразу:
- `networkedJob.JobBooklet` остался null → `ItemNetID = 0` ✓ (совпадает с логом);
- `TrackedItemType` остался null → `DoNotCreateItem(null)` возвращает **false** → бланк
  поехал клиенту как **обычный предмет**, с `Create` ✓ (объясняет, почему клиент его видит);
- клиент им распоряжается как любым предметом ✓ (объясняет мусорку).

**Проверять так:** взять джоб хостом и посмотреть хост-лог на
`CreateJobBooklet() NetworkedJob not found`. Если строка есть — гипотеза верна.

**Частично поправлено в 0.1.21.0:** мой `GenerateBooklet` создавал бланк **всем**, а не только
владельцу — это противоречило уже существующему решению в `Server_TakeJob` («хост прячет
бланк, если джоб не его»). Теперь только владельцу. Но корень (`owner id 0`) не тронут.

**Ещё не решено:** почему `owner id 0`. Владельца ставит `ProcessJobOverview_Postfix` через
`HostPlayer()` → `server.TryGetServerPlayer(server.SelfId)`, где
`SelfId => Client?.PlayerId ?? 0`. Если хост берёт джоб раньше, чем его собственный локальный
клиент получил `PlayerId`, `HostPlayer()` вернёт null и владелец не назначится. В прошлой
сессии (0.1.19.0) тот же джоб имел `owner id 1` — то есть плавает.

#### Б22: движущиеся предметы синхронизируются только по событию — этого мало

**Твоя идея (17.07): «может тиком просто показывать положение, как игроки?» — верная.**

Б20 починил **систему координат**, но не **частоту**. Снапшот шлётся только на смену
состояния, а «лежит и трясётся в едущей кабине» — не смена состояния: `GetSnapshot()` вернёт
`null`, и клиент дальше крутит свою физику от последней известной точки. Отсюда «предметы не в
тех положениях» даже после Б20.

**Сделано в 0.1.21.0 — стрим состояния движущихся предметов:**

- В enum `ItemUpdateType` флаг `ItemPosition = 8` уже был, но **в одиночку не отправлялся
  никогда** — только внутри `FullSync`. Теперь `ItemPosition` сам по себе = «поток движения»:
  вагон + позиция + поворот + **скорость и угловая скорость**, без состояния и без префаба.
- Хост стримит предмет, **пока тот движется** (`settleWatch && settleSeenMoving`), и умолкает,
  как только тот встал. Покой считается **относительно вагона** — предмет, спокойно лежащий в
  едущей кабине, не движется и не стримится.
- Скорость тоже передаётся в системе отсчёта вагона (за вычетом скорости самого вагона), так
  что приёмник продолжает симуляцию из того же состояния, а не телепортируется рывками.
- `CheckSettled` больше не сдаётся по таймауту, пока предмет движется: предмет, который не
  останавливается, — это ровно тот, о котором надо рассказывать, а не махнуть рукой.
- Приём стрима не трогает состояние, не размораживает и не перебрасывает предмет — иначе
  `grabHandler.Throw()` отрабатывал бы раз в тик.
- Устаревший стрим не ставится в очередь: позиция секундной давности хуже, чем никакая.

**Почему это не залило сеть:** интерес-менеджмент уже был — `ProcessChanged` шлёт только
игрокам, у которых предмет в `NearbyItems` (100 м). Стримятся только движущиеся предметы
рядом с тобой. Один предмет в движении ≈ 50 байт × 24 Гц ≈ 1,2 КБ/с.

#### Пятый прогон (17.07, 0.1.21.0) и фиксы 0.1.21.1

**Про логи — важно и повторяется.** Присланные `Player_Client.log` и `Player_Host.log` — это
**один и тот же файл**, скопированный дважды с разницей в 13 с (совпадают номера строк, время
и содержимое; второй просто длиннее). Обе копии игры пишут в **один** `Player.log`: LocalLow
общий для всех установок. Поэтому в файле перемешан вывод обоих процессов — есть и
`CacheWorldItems` (только клиент), и `[Server] Jobs: refused` (только хост). Различать можно
по префиксам `[Server]`/`[Client]`, но строки `[Diag]` без префикса — уже никак.

**Решение — в моде уже есть, и второе устройство не нужно.** `Multiplayer.WriteLog()` пишет
в `LOG_FILE = "multiplayer.log"` **относительным** путём, то есть рядом с `DerailValley.exe`,
а установки у нас разные — значит лог получается per-install сам собой. Включается галкой
**Enable Log File** в Advanced-настройках UMM, чистится при каждом запуске и содержит только
строки мода. Вместе с **Debug Logging** это и есть правильный способ снимать логи; подробности
в TESTING.md. Отдельные машины ни при чём: путь `Player.log` привязан к продукту (LocalLow),
а не к установке.

**Гипотеза по Б21 не подтвердилась.** Строки `CreateJobBooklet() NetworkedJob not found` в
логе **нет**. Значит бланк уезжает клиенту не через сломанный `Initialize()`.

**И вторая моя ошибка, тут же:** я записал «`owner id 0` у всех шести джобов, владелец не
назначается никогда». Перепроверил строки — все шесть это `-> Expired`, то есть джобы,
которые никто не брал, и `owner id 0` для них **правильно**. Улика была ровно одна, из
сессии 0.1.20.0: `joining onto in-progress HB-SL-32, owner id 0`. Пятый случай в копилку
«плану нельзя верить»: цифра сама по себе ничего не значит, смотри на строку целиком.

**Зато настоящий корень нашёлся, и он давно записан.** Тот же джоб `HB-SL-32` в сессии
0.1.19.0 имел `owner id 1`, а в 0.1.20.0 — `owner id 0`. Один сейв, разные запуски. Это
**Б9**: `OwnedBy` присваивается в `SetOwner()` и **больше нигде не читается** — ни
сериализации, ни персиста. Хост взял джоб → перезапустился → в сейве джоб остался
`InProgress`, владелец испарился → бланк ничей → любой может выкинуть его в мусорку и
отменить чужую работу.

**Б21 — это не отдельный баг, а последствие Б9.** И оно поднимает Б9 в приоритете: это уже
не «джоб некому работать», а «чужую работу может отменить кто угодно».

**Ложный след, за которым не пошёл:** `TrainCar L-079 failed to load an interior`. Выглядит
многообещающе (симптом-то про вагон), но это наш `WaitForInterior`, который ждёт
`TrainCar.loadedInterior` для хука органов управления. `loadedInterior` выгружается, когда
игрок выходит из кабины, — а `ItemReparentingBase` парентит предметы к `trainCar.interior`,
это **другое поле** и оно есть всегда. К предметам отношения не имеет.

**Ошибок предметов в логе ноль**, отказов валидации — тоже ноль.

#### Б23: ушёл за 100 м и вернулся — предметов больше нет

Найдено в игре 17.07: «отошёл клиентом, потом запрыгнул в вагон к хосту — предметы не видны,
**но всё, что при мне кидали, видно**».

**Вторая половина симптома и есть улика.** Брошенный предмет шлёт `ItemState`, а его приём
делает `SetActive(true)` — предмет **появляется**. Значит предметы у клиента **есть**, просто
выключены или смещены, и никто их не поправляет.

**Причина найдена чтением:** интерес-менеджмент сделан наполовину. `UpdatePlayerItemLists`
выкидывает предмет из `NearbyItems` через 3 с после ухода — но `KnownItems` **не чистит
никогда**. Пока тебя нет, хост тебе не рассказывает ничего; при этом всё, что за это время
случилось с твоей копией (игра её спрятала, `RespawnOnDrop` убрал, LOD выгрузил), хост не
видит и поправить не может. А вернёшься — `KnownItems` по-прежнему клянётся, что предмет у
тебя есть, и `Create` больше **не придёт никогда**.

**Починено в 0.1.21.1:** уход из радиуса чистит и `KnownItems`. Возвращение заново
объявляет то, что там есть.

#### Б24: отказ валидации ничего не исправляет

Найдено при разборе вопроса «может запретить поднимать чужие предметы?».

**Запрет уже есть, и он не нужен.** Предмет в руках другого игрока у тебя `SetActive(false)` —
его физически не видно и не взять. Плюс `ValidatePlayerAction` отказывает, если предметом
владеет другой.

**Настоящая дыра — отказ немой.** Сервер отверг снапшот и вышел; клиент об этом не узнаёт и
продолжает держать предмет, который для всех остальных лежит на земле. Ничто это не
исправляет — ровно та же болезнь, что была у Б8 с джобами.

**Починено в 0.1.21.1:** отказ помечает предмет грязным (`MarkRelayDirty()`), и
`ProcessChanged` досылает отказанному игроку `FullSync` с тем, как есть на самом деле. Копия
возвращается к правде сама.

#### Б25: стрим движения дёргает предметы

Моя же регрессия из 0.1.21.0: `ApplyMotionStream` ставил `transform.position` жёстко 24 раза
в секунду. Две физики никогда не совпадают до миллиметра, и каждое расхождение выглядело как
рывок. Игроки в моде интерполируются, предметы — нет.

**Починено в 0.1.21.1:** скорость отдаём физике (она и ведёт предмет между пакетами), а
позицию — сглаживаем `Lerp`/`Slerp`. Жёсткий снап остаётся только когда копии разошлись
больше чем на 0.5 м, то есть это уже не расхождение, а ошибка.

#### Б16: предмет зависает в воздухе — забыли разморозить после инвентаря

Симптом (найден в игре 16.07): личную вещь бросили, второй игрок подобрал, владелец забрал
обратно в инвентарь — у подобравшего пропала, **а у владельца висит в воздухе**. И отдельно:
«если кинуть предмет, **который вернули в инвентарь**, он пропадает при выкидывании, а у
владельца зависает в воздухе».

**Механизм — доказан декомпилятором.** Предмет в инвентаре игры — это не «спрятанный
предмет», а замороженный. `DV.InventorySystem.Inventory.FinalizeRemoveItemFromWorld()`:

```csharp
AssignInventoryLayer(item);              // слой меняется на инвентарный
component.collisionDetectionMode = CollisionDetectionMode.Discrete;
component.isKinematic = true;            // физика выключена
item.SetActive(value: false);
```

Обратная операция у игры — `Inventory.ReturnItemToWorld()`, и она делает ровно две вещи,
которые мы **не делаем**: `isKinematic = false` и `AssignWorldItemLayer(item)`.

Наш `HandleDroppedOrThrownState()` при этом только `SetActive(true)` + ставит позицию.
Кинематическое тело остаётся кинематическим, значит:

* гравитация на него **не действует** → предмет висит там, куда его поставил снапшот;
* `grabHandler.Throw()` → `AddForce()` по кинематическому телу — **пустая операция**, бросок
  просто теряется;
* обнуление скорости из 0.1.18.1 у меня под `if (!rb.isKinematic)` — тоже мимо;
* слой остаётся инвентарным — предмет, вероятно, ещё и не подобрать.

Это **наш** баг, не игры, и это упущение, а не решение: соседний `HandleAttachedState()`
ровно эту строку делает — `Item.ItemRigidbody.isKinematic = false;` перед привязкой.
Ключ к симптому дал сам репорт: «предмет, который **вернули в инвентарь**» — это ровно тот
предмет, что прошёл через `FinalizeRemoveItemFromWorld` и стал кинематическим. У владельца
личная вещь лежит в его собственном поясном слоте (слоты 14–20, см. Б13) — то есть
заморожена; приходит снапшот `Dropped` → `SetActive(true)` + позиция → **висит в воздухе**.

**Как чинить (дёшево и по-игровому).** В `HandleDroppedOrThrownState()`, до `SetActive`:

```csharp
Inventory.Instance.ReturnItemToWorld(gameObject, false);   // activate: false!
```

`activate: false` обязателен: с `true` игра телепортирует предмет к **локальному** игроку
(`item.transform.position = playerTransform.position`), а позицию мы ставим сами из снапшота.

**Что было записано здесь раньше и оказалось неверным.** Сначала я списал этот симптом на
`RespawnOnDrop` — и это была ошибка от излишней уверенности. Разбор ниже верен по фактам, но
пороги там **200 м** (личные) / 1000 м (прочие), то есть при броске через комнату он не
срабатывает вовсе и объяснить симптом не мог. Четвёртый случай в копилку «плану нельзя
верить»: проверяй не только «что делает метод», но и **при каких числах он вообще запускается**.

#### Б17: `RespawnOnDrop` воюет с синком (латентный, стреляет на 200+ м)

Не причина Б16, но реальный баг — просто далеко. У `RespawnOnDrop` крутится корутина
`Checker(0.2f)` **на каждой машине независимо**, и она делает две несинхронизированные вещи:

1. **Морозит:** `item.ItemRigidbody.isKinematic = farFromActiveCamera` — предмет дальше
   `maxDistance` от **локальной** камеры становится кинематическим. Никакого пакета не шлётся.
2. **Прячет и телепортирует:** `farFromSpawn && farFromPlayer` → `RespawnOrDestroy` →
   для личной вещи `SetActive(false)` + `SetParent(WorldMover.OriginShiftParent)` +
   `localPosition = Vector3.zero` + `AddItemToLostAndFound(item)`. Локально, без снапшота.
   Если `respawnOnDropThroughFloor == false` — предмет вообще
   `DV_GameObjectDestructionHandler.RemoveGameObject()`, то есть **уничтожается**.

Зачем это в игре: страховка «предмет провалился сквозь пол / уехал на вагоне» — обычный
предмет телепортируется на точку спавна, личный уходит в Lost & Found, чтобы его можно было
забрать со склада. В одиночке «далеко от игрока» — глобальная правда, потому что игрок один.

Пороги: `STANDARD_ESSENTIAL_RESPAWN_DISTANCE = 200f` (личные), 1000 м (прочие) —
`maxDistanceSquared` присваивается **только** в `SetMaxDistance()`, а тот зовётся из
`Initialize()` при `shouldSetDefaultRespawnDistance`.

Почему по личным вещам больнее: `ignoreDistanceFromSpawnPosition = !OnValidRespawnParent &&
item.InventorySpecs.BelongsToPlayer` — у личной вещи не на вагоне это `true`, и тогда
`CheckDistances()` отдаёт `farFromSpawn = float.PositiveInfinity > max`, то есть **всегда
true**. Условие схлопывается до одного «дальше 200 м от **моего** игрока».

Почему у клиента страдают только личные вещи: `SendToCache` сносит компонент
(`Destroy(respawn)`) — но только у **кешированных**. Личные вещи в кеш не попадают никогда
(`IsEssential()` пропускают и `CacheWorldItems`, и `ShouldSuppressLocalItem`), а **хост не
кеширует вообще ничего** (`CacheWorldItems` выходит первой строкой у хоста) — у хоста
`RespawnOnDrop` жив на **каждом** предмете.

**Мод это предвидел:** `RespawnOnDropPatch` — транспайлер, вырезающий ветку `SetActive(false)`
+ `AddItemToLostAndFound`. Написан целиком и **выключен первой строкой**:
`return codes; //disable pactch temporarily`. Но даже включённый он глушит только «спрятать»,
а заморозку `isKinematic` не трогает.

Варианты, когда дойдут руки: **A** — снять `RespawnOnDrop` с сетевых предметов (ценой
страховки «провалился сквозь пол»); **B** — включить транспайлер (хрупкий IL, половинчато);
**C** — оставить только хосту и слать результат снапшотом (дороже всего).

#### Про идею «кто взял бумажку, тот и командует джобой»

Идея хорошая и большая часть фундамента под неё уже есть — но это **отдельный этап**, не
делаю сейчас. Что уже готово: событие на подбор существует (`ItemBase.Grabbed`/`Ungrabbed`
→ `OnGrabbed`/`OnUngrabbed` помечают предмет грязным), владелец предмета уже считается
(`ServerPlayer.AddOwnedItem`/`OwnsItem`, `NetworkedItem.OwnerId`), и `NetworkedJob` уже
знает `OwnedBy`. **С 0.1.19.0 половина сделана:** `ValidatePlayerAction` включён, владение
предметом считается по-настоящему и подделать отправителя больше нельзя (Б6). Осталось одно:
связать владение бумажкой с владением джобом — на `InHand` бумажки назначать
`NetworkedJob.OwnedBy` по владельцу предмета. Это следующий заход, после проверки 0.1.19.0
в игре.

#### Дальше по этому пункту

**Этап 3 — сделано в 0.1.19.0.** Заглушка валидации снята, Б5 и Б6 закрыты тем же заходом.
Ключевое, что делает включение безопасным: **предмет без владельца пропускается всегда**.
Владение живёт только в памяти сервера, поэтому «ничей = запрещено» намертво заперло бы
предметы, которые никто уже не смог бы ни поднять, ни положить. Владелец же может положить
предмет всегда (`currentOwner == player`), так что «залип в руках» больше не наступает.
Отказы логируются `LogWarning` с дистанцией и именами — если в игре начнутся отказы,
причина будет прямо в логе.

**Этап 4 — сделано в 0.1.19.0 (Б10).** Выбрали «падают на землю»: предметы ушедшего
возвращаются в мир на месте выхода (+1 м вверх, чтобы не воткнуть в грунт) через
`ReleaseFromDisconnectedOwner` → `FullSync` оставшимся. Причина, по которой обработчик был
мёртв, оказалась прозаичной: в B99 событие стало `Action<ServerPlayer>`, а обработчик остался
с `uint netID` — сигнатуры не совпали, и подписку закомментировали.

**Этап 5. Lost & Found — у каждого свой.**
В сторожке есть кнопка возврата потерянных предметов. Ванилла: `StorageAccessPointLostAndFound`
→ `StorageController.RequestLostAndFoundItemActivation()`, хранилище — `StorageLostAndFound`,
наполняется через `AddItemToLostAndFound` / `MoveItemsFromWorldToLostAndFound`.

Мод это **не трогает вообще**: `StorageControllerPatch` целиком закомментирован (там был
только отладочный лог стектрейсов).

Сейчас `StorageController` — синглтон на каждом клиенте, то есть Lost & Found у каждого свой
и локальный. Как только предметы станут общими, вопрос «чьи потерянные» станет настоящим:
нажал кнопку — вернуться должны **твои**, а не всё, что мир потерял.

Заготовка есть: `ServerPlayer.Storage` (`StorageBase`) объявлен и не используется — как
`OwnedBy` у джобов. Туда и складывать. Кнопка → серверный запрос → сервер возвращает предметы
этого игрока.

Тут же всплывёт вопрос владения: предмет, который ты никогда не брал, но который потерялся
в мире — чей он? Скорее всего Lost & Found делится на «мои потерянные» (я держал, `OwnedBy`)
и «ничьи из мира» (общие, возвращаются кому угодно).

**Этап 6. «Вижу предмет в чужой руке».** Сейчас взятый предмет просто гасится
(`gameObject.SetActive(false)`, `NetworkedItem.cs:622`) — «со стола пропал» этим и достигается,
а вот в руке его не видно. В коде стоит `//todo add to player model's hand`. Нужна точка
крепления на модели игрока и обновление позиции предмета по руке владельца.

#### VR и ПК

Разницы для синхронизации быть не должно: она **по состоянию** (`InHand`/`Dropped`/…), а не
по механике захвата. VR-игрок хватает иначе, но снапшот выходит тот же. `ServerPlayer.IsVR`
уже есть. Разница вылезет только на этапе 6: точка крепления в руке у VR-модели своя.

#### Чего это НЕ чинит

Бумажки джобов синхронизацией предметов **не заведуются** — джоб-система создаёт и уничтожает
их сама, по job-пакетам (см. «Личные джобы»). Они уже работают правильно и трогать их не надо.
Синхронизация нужна для остального: лопаты, фонари, маслёнки, банкноты, бумажки лицензий (Б3).

#### Идея: бланк и есть владение (твоя, 16.07)

«Не париться с синхронизацией джобов, а синхронизировать предметы: кто вставил — того и
джоб, кто подобрал — того и бланк.»

**Идея хорошая, и она снимает целый класс проблем.** Сейчас владение джобом — отдельная
бухгалтерия (`OwnedBy`/`OwnerId`, наборы на `ServerPlayer`, проброс владельца в двух
структурах пакетов), которая живёт **параллельно** физическому бланку и может с ним
разъехаться. Уже разъезжалась: без бланка не уходил его NetId, и клиент решал, что ему
отказали, хотя джоб он получил.

Если предметы синхронизированы честно, бланк **сам** становится токеном владения: он один на
мир, лежит у кого-то в руках или в кармане, и «чей джоб» — это просто «у кого бланк».
Отдельное поле владельца тогда нужно только серверу для проверок, а не как источник правды.

Что это даёт помимо простоты:
- **Передача джоба** появляется бесплатно: отдал бланк — отдал джоб.
- Пропадает рассинхрон «джоб мой, а бланка нет» — он физически невозможен.
- Не нужен `OwnerId` в `JobData` и `JobUpdateStruct`.

Чего не снимает: сервер всё равно должен судить **по игроку** (его лицензии, его слоты) в
момент вставки бланка, и всё равно должен знать, кому платить за сдачу. То есть `OwnedBy`
остаётся, но перестаёт быть тем, что клиент выясняет по проводу.

**Порядок от этого не меняется:** синхронизация предметов всё равно первая. Переводить джобы
на «бланк = владение» имеет смысл только после неё — тогда и посмотреть, что из нынешней
бухгалтерии можно выкинуть.

#### Заготовка в апстриме

`upstream/feature/item-sync` — ~300 строк, «Begin player inventory sync», про персист
инвентаря/ящиков. Это **другая** задача (что лежит у игрока в карманах между сессиями), а не
синхронизация мира. Смотреть, но не ждать от неё решения этого пункта.

### 2. Перевод денег между игроками

Пока отложено по твоему решению. Нужен для выхода новичка из тупика, если стартовую
сумму опять срежем.

- новый `ServerboundMoneyTransferRequestPacket` (получатель + сумма)
- валидация на сервере: сумма > 0, хватает средств, получатель существует, не сам себе, атомарно
- UI: кнопка в `Multiplayer/Components/Networking/UI/PlayerListGUI.cs` (меню по Alt),
  окно ввода суммы — паттерн IMGUI брать из соседнего `ChatGUI.cs`

### 3. Джобы: доделать

**Ровно та же болезнь, что была у лицензий, и теперь она вылезла наружу.** Клиентский
префикс `ProcessJobOverview` возвращает `false` и шлёт запрос, а сервер зовёт ванильный
`ProcessJobOverview` — **в игре хоста**. Ванилла там проверяет три вещи, и все три хостовы:

- `LicenseManager.Instance.IsLicensedForJob(...)` — лицензии хоста
- `JobsManager.Instance.currentJobs.Count >= GetNumberOfAllowedConcurrentJobs()` — слоты хоста
- `CareerManagerDebtController.Instance.IsPlayerAllowedToTakeJob()` — долги хоста

И `TakeJobFromStation` кладёт джоб в `currentJobs` **хоста**.

Пока лицензии были общими, это совпадало и не было видно. Теперь противоречие прямое:
клиент купил лицензию, которой нет у хоста, — джоб не возьмёт; лицензии нет, а у хоста есть —
возьмёт. Лимит одновременных джобов общий на всех.

`NetworkedJob.OwnedBy` объявлен и **нигде не используется** — мёртвая заглушка под это.
Джобы рассылаются броадкастом (`SendJobsCreatePacket`/`SendJobsUpdatePacket` → `SendPacketToAll`).

Лечится тем же приёмом, что лицензии: не пускать чужое действие через синглтоны хоста.
Нужны наборы взятых джобов на `ServerPlayer` и проверки против **его** лицензий/слотов/долгов,
а не хостовых. `OwnedBy` — готовое место для владельца.

Наработки в `upstream/feature/sync-jobs` — 14 коммитов, 1627 строк:
`StationPatch`, `StationJobGenerationRangePatch`, фиксы спавна бланков, синхронизация табличек.

**Не мержить целиком.** Ветка отстаёт от `beta` на 587 коммитов, пробный мерж дал
20 конфликтов из 29 файлов, и большинство не про джобы (`ServerBrowserPane`,
`NetworkedTrainCar`, `Settings`, `locale.csv`) — это год расхождения, а не полезная работа.
Переносить прицельно, кусками, поверх свежей `beta`.

Учесть: `sync-jobs` делает джобы **общими и согласованными**, а нужно — **личными**.
Фундамент оттуда полезен, часть логики придётся разворачивать.

Оплата за джоб уже адресная — идёт тому, кто сдал бланк (см. «Сделано»). Осталось владение
самим джобом: чей он, кто может его сдать.

### 4. Поезда: личные

**Отложено твоим решением.** Один блокер снят: долг за разбитый локомотив — на **владельце**,
том, кто вызвал (даже если разбил другой). Сюда же гаражи (Б4) — они часть этой темы, а не
лицензий. Самый запутанный пункт: долги, гаражи, владение вагонами.
`NetworkedCareerManagerDebtController` уже существует.

#### 4.1. Восстановление паровозов — личное у каждого (задача от 17.07)

**Чего хотим.** Сейчас восстановление — гонка: кто первым продвинул, тот и владелец, для
остальных паровоз уже занят. Надо, чтобы **каждый мог завладеть своим**: собираемые части и
сам паровоз, подобранные одним игроком, у него исчезают (он их забрал), а для второго
остаются доступными — у второго своя копия прогресса.

**Как устроено в игре (декомпилятор, 17.07):**

| Что | Где |
|---|---|
| Автомат состояний, **одна штука на ливрею** паровоза | `DV.LocoRestoration.LocoRestorationController` (сценовый MonoBehaviour) |
| Состояния S0…S10 | `RestorationState`: `S0_Initialized` → `S1_UnlockedRestorationLicense` → `S2_LocoUnblocked` → `S3_RerailedCars` → `S4_OnDestinationTrack` → `S5_PartOrdered` → `S6_PartPickedUp` → `S7_PartDelivered` → `S8_PartInstalled` → `S9_LocoServiced` → `S10_PaintJobDone` |
| Текущее состояние | `LocoRestorationController.State`, пишется в `saveData.SetInt("state", …)` |
| Персист | `GetSaveData()` — один JObject на всю игру, ключ `SaveID` = `locoLivery.id`. **Игрока в ключе нет** |
| Все контроллеры | `static List<LocoRestorationController> allLocoRestorationControllers` |
| Событие смены | `event Action<LocoRestorationController, TrainCarLivery, RestorationState> StateChanged` |

**Как устроено в моде сейчас:**

- `NetworkRestorationWatcher` (только у хоста, `AllowAutoCreate` возвращает null у клиента)
  подписан на `StateChanged` и **броадкастит всем**:
  `Server.SendRestorationStateChange(netId, newState, transportCars)`.
- То есть прогресс **глобальный и общий**: продвинул один — продвинулось у всех. Ровно то,
  на что ты жалуешься.
- `LocoRestorationControllerPatch` чинит только две несвязанные вещи: гейт готовности мира на
  старте и дистанцию до всплывашки. Про владение там нет ничего.

**Почему это тяжелее, чем джобы и лицензии.** Личные лицензии — это данные, их размножить
дёшево. Тут же завязка на **физику общего мира**:

1. **Паровоз — реальный `TrainCar`** (`controller.loco`, спавнится `garageSpawner`). Он один.
   «У каждого свой» означает либо отдельный физический паровоз на игрока (а это уже не общий
   мир), либо персональную видимость одного и того же вагона — очень инвазивно.
2. **«Собираемые предметы» — это не предметы из инвентаря, а карго**: `locoPartCargo`
   (`CargoType_v2`), выдаётся через `warehouseMachineForPartPickup`
   (`WarehouseMachineController`) и едет на вагонах (`controller.transportingCars`). То есть
   к синхронизации предметов (п.1) это **не относится**, там своя система.
3. **Персист не знает про игрока** — ключ только `locoLivery.id`. Личный прогресс требует
   `Guid игрока → состояние` на каждую ливрею, как сделано с кошельками и лицензиями.

**Прямо противоречит** нашей же модели «мир общий, прогресс личный»: паровоз и карго —
это мир, а не прогресс.

**С чего начинать, когда дойдут руки:**

1. Сначала ответить на открытый вопрос 2 («личный поезд» — это что?), иначе не спроектировать.
2. Развилка, которую надо выбрать **до** кода:
   - **Инстансы:** у каждого свой физический паровоз и своё карго (видит только он).
     Честно отвечает твоему запросу, но это инстансинг куска мира — самое дорогое.
   - **Только прогресс личный, железо общее:** состояние S0…S10 у каждого своё, но паровоз
     физически один. Дёшево, но два игрока подерутся за один и тот же вагон.
   - **Общий кооп:** восстанавливаем вместе, владение — у того, кто заплатил. Дешевле всего,
     но это не то, что ты просишь.
3. Что бы ни выбрали, `NetworkRestorationWatcher` перестаёт быть броадкастом, а
   `RestorationState` переезжает в `ServerPlayer` (рядом с кошельком и лицензиями) с ключом
   по `locoLivery.id`.

---

## Открытые вопросы

1. **Бумажка после применения.** Джоб стал моим, бумажка физически осталась предметом.
   Другой игрок может её взять? Сдать мой джоб в кассу? Выбросить?
   *Рекомендация: защищать, иначе саботаж тривиален.*

2. **«Личный поезд» — это что?** Право водить? Право удалить? Или только «кто заплатил»?
   *Открыт. Без него п.4 не проектируется.*
   **Направление задано 17.07:** каждый должен иметь возможность завладеть **своим** —
   восстановление не гонка «кто первый». Но конкретный механизм (свой физический паровоз
   у каждого / общее железо + личный прогресс / кооп) не выбран, а он и есть главная
   развилка. См. п.4.1.

3. ~~**Долги.** Игрок A вызвал локомотив за свои, игрок B его разбил — чей долг?~~
   **Отвечено: долг владельца (A)** — кто вызвал, тот и отвечает. Просто в коде, но открывает
   саботаж: разбил чужой — платит владелец. Учесть при проектировании п.4.

## Ветки

| Ветка | Состояние |
|---|---|
| `beta` (форк, `origin`) | **основная.** Личные профили влиты 17.07: кошельки, лицензии, джобы, отказ покупки, награда за джоб. `feature/per-player-money` влита сюда fast-forward и удалена |
| `feature/item-sync` (форк) | **рабочая.** Предметы: Б5, Б6, Б10, Б14, Б15, Б16 закрыты в 0.1.19.0 |
| `upstream/feature/sync-jobs` | 14 коммитов ценного, отстаёт от beta на 587, брошена 06.2025 |
| `upstream/feature/item-sync` | заготовка ~300 строк, «Begin player inventory sync», про персист инвентаря/ящиков |

## Сборка

`dotnet build Multiplayer.sln -c Release` из корня. Версия в UMM подтверждает свою сборку.

Оговорки, на которые уходит время:

- Проект `MultiplayerAPI Tests` не собирается (нет namespace `MPAPI`) — это не наша поломка.
  Собирать `Multiplayer/Multiplayer.csproj` отдельно.
- `MultiplayerEditor.dll` и `UnityChan.dll` лежат в `<DvInstallDir>\Mods\Multiplayer`, а не в
  `Managed`. Основной репозиторий собирается только потому, что они уже осели в его `bin/`.
  В свежем клоне/воркtree добавь `$(DvInstallDir)\Mods\Multiplayer\` в `ReferencePath`
  в своём `Directory.Build.targets`.
- `post-build.ps1` копирует мод в `<DvInstallDir>\Mods\Multiplayer` и делает
  `Releases\Multiplayer <ver>.zip` — теперь и из `dotnet` CLI тоже. Раньше только из Visual
  Studio: условия висели на `$(ConfigurationName)`, а это свойство VS, в CLI оно пустое и
  копирование молча не срабатывало. Заменено на стандартное `$(Configuration)`.
- Следствие: любая сборка перезаписывает мод в игре. Если игра запущена, файл может быть
  примаплен, `Copy-Item` падает и сборка валится с `MSB3073` — закрывай игру перед сборкой.
  Падение **перемежающееся**: зависит от того, что игра держит в этот момент. Проверено
  17.07 — при запущенной игре одна и та же команда дала то `Ошибок: 1`, то `Ошибок: 0`,
  причём `Multiplayer.dll` при этом скопировался. Не гадай: если сомневаешься, свежий ли мод
  в игре, сверь время файла в `<DvInstallDir>\Mods\Multiplayer\Multiplayer.dll`.
- Zip из `Releases/` ставится у другого человека через Unity Mod Manager. Версии должны
  совпадать у всех: мод сверяет их при входе, а формат пакетов мы меняем.
